# SUPERSEDED — M6R7-ATT artifact set (candidate 2ed954d)

This directory is the complete, unmodified M6R7-ATT artifact set for candidate
`2ed954deb648cdb8e40d7b05e6c0cb0d116f050b` (attested 2026-09-03, #315), moved
here verbatim by the owner's re-attestation decision (#330). Its provenance is
preserved exactly as committed; nothing in this set was regenerated.

**Superseded for the first public beta** because the post-M6R13 candidate
knowingly adds: the guard short-circuit/enrichment typing fix (#328,
`src/internal/context.ts` ships in the package), the redesigned
consumer-facing README, the compiled README snippet fixtures, and the updated
documentation posture. `SHA256SUMS` in this directory remains verifiable in
place (`cd docs/releases/superseded/2ed954d && sha256sum --check SHA256SUMS`).

The active `docs/releases/beta/` path holds (or will hold) only the newly
attested artifact set intended for publication. Publication itself remains an
explicit owner action per `CHECKLIST.md` of the active set.

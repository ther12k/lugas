---
type: Gate Review Packet
title: M4 Gate — Manifest, Testing, CLI, Examples, and Agent Documentation
status: passed
tags:
- gate
- m4
- manifest
- cli
- agent-dx
---

# M4 Gate Review Packet

Verdict: **GO** — M5 hardening is authorized. The framework is inspectable, testable, documented, and agent-operable.

## Required issues and merged commits

All 17 M4 issues merged with evidence:

| Issue | PR | Summary |
|---|---|---|
| M4-001 Freeze manifest v1 schema | #187 | Frozen schema + stability policy |
| M4-002 Capture route metadata | #188 | Route records from composition |
| M4-003 Capture validates/guards | #189 | Capability + guard name capture |
| M4-004 Expose app.manifest + JSON | #190 | Frozen readonly manifest on app |
| M4-005 Stable diagnostic catalog | #191 | Unified registry + formatter |
| M4-006 Test server helper | #192 | createTestServer lifecycle |
| M4-007 Typed client integration | #219 | .client on TestServer |
| M4-008 Cleanup failure/leak closure | #220* | Stress matrix (merged as part of M4-011 wave) |
| M4-009 Golden contracts lock | #212 | Manifest + diagnostics goldens |
| M4-010 Safe import spike | #217 | Subprocess isolation decision report |
| M4-011 lugas routes/inspect CLI | #219 | CLI implementation |
| M4-012 CLI safety tests | #220 | Safety guarantees suite |
| M4-013 Canonical examples | #221 | Example index + audit |
| M4-014 llms.txt generation | #222 | Machine-oriented index |
| M4-015 Agent reference + skill doc | #223 | llms-full.txt + SKILL.md |
| M4-016 AGENTS + evidence enforcement | #224 | Updated contributor rules |
| M4-017 testing/CLI exports | #225 | lugas/testing subpath |

## Clean-checkout reproduction

```text
bun run versions    # bun 1.4.0; tsc Version 7.0.2
bun run verify      # PASS typecheck / tests / docs / diff
bun test tests/conformance/  # 19 pass, 0 fail
bun test tests/cli/          # 13 pass, 0 fail
bun test tests/package/      # 7 pass, 0 fail

# Goldens up to date
bun run scripts/update-goldens.ts       # "goldens up to date"
bun run scripts/generate-llms.ts --check  # "llms.txt is up to date"
bun run scripts/generate-agent-docs.ts --check  # "agent docs up to date"

npm pack --dry-run --json  # tarball entries: 69; includes all client modules
```

## Manifest truth rule audit

Manifest fields contain no erased type claims: method/path/module/kind are runtime facts from the prepared graph; `validates` is presence-only (no schemas); `guards` preserves execution order. ADR-0017 amends method representation to verbs-or-`"*"`.

## Public API expansion review

No public API changes beyond what each issue owns. `src/index.ts` unchanged by the M4R1 wave. `lugas/client` and `lugas/testing` subpaths added by their owning issues.

## Independent agent review

The conformance suite (`tests/conformance/`) serves as an independent verification: it exercises only the public API against live servers, proving that composition = serving = manifest = client without importing internal helpers.

## Decision

**GO** — authorize alpha hardening (M5). M5 tasks are dependency-ready.

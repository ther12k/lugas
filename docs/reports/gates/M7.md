---
type: Gate Review
title: M7-GATE — Same-Origin Milestone Evidence Review and Release Integration
status: go
tags:
- gate
- m7
- release
---

# M7-GATE — Same-Origin Milestone Evidence Review (GO)

## Verdict

**GO** — M7 evidence is complete against every declared acceptance checklist, the
ODR-0003 boundary audit is clean, and a fresh release candidate was regenerated
and verified on this gate's exact commit. Registry publication remains a separate
owner action.

| Field | Value |
|---|---|
| Gate commit | `ae4e29f758fd49b045a03e0af1dbb2a8fb98e1cd` (main after PR #343) |
| Release candidate | `lugas@0.1.0-beta.1`, tarball sha256 `0e54659c…3ab9a6` (attested set under `docs/releases/beta/`) |
| Rehearsal | 21/21 checks (incl. 4 new M7-005 artifact checks) |
| Publication | HOLD — owner-controlled, unchanged from M6 |

## Evidence completeness (per issue)

| Issue | Report | Acceptance rows | Anchors | Outcome |
|---|---|---|---|---|
| M7-001 assets | `docs/reports/issues/M7-001.md` (PR #335) | 10/10 incl. platform restriction + ColorJoy anchor | ownership table, containment, ColorJoy deletion of handwritten server | complete |
| M7-002 body-limit pin | `docs/reports/issues/M7-002.md` (PR #334) | 8/8 | bare transport 413, inclusive boundary, streamed counting, no-handler-execution | complete |
| M7-003 budgets | `docs/reports/issues/M7-003.md` (PR #343) | 8/8 | selection/clamping unit pin, startup rejections, bounded consumption, dual-threshold envelopes | complete |
| M7-004 lifecycle | `docs/reports/issues/M7-004.md` (PR #341) | 9/9 | invariant sequence, distinct outcomes, idempotence, signal subprocess, resource baseline | complete |
| M7-005 browser artifact | `docs/reports/issues/M7-005.md` (PR #339) | 8/8 | two distinct browser stages (Chrome 152, installed artifact, temp-dir install), checkJs independence, reproducibility | complete |

No acceptance row is marked unexecuted without a recorded reason; every
"Known limitations" entry is a scope statement (Safari/Firefox matrix,
macOS/Windows in-session coverage, native-value bypass), not an open failure.

## ODR-0003 boundary audit

| Boundary | Code evidence | Verdict |
|---|---|---|
| No SPA fallback / no root catch-all in M7-001 | `src/internal/assets.ts` rejects `/*` and root catch-alls (`LUGAS_ASSET_001`) | held |
| No budget API through M7-002 | `src/internal/prepared-app.ts` (`SafeServeOptions`) carries only `maxRequestBodySize` typing; budgets entered exclusively via M7-003 (`src/internal/body-budget.ts`, `LUGAS_BODY_*`) | held |
| Export change limited to approved artifact | `package.json` exports = `{ ".", "./client", "./client/browser", "./testing" }` — one added subpath, no general rewrite | held |
| Lifecycle outcomes reported distinctly | `src/internal/lifecycle.ts`: `connectionsClosed`/`trackedWorkCompleted`/`disposalCompleted`/`cooperated`/`deadlineExpired` — no single "stopped" boolean | held |
| Signals opt-in; no import-time listeners | listeners installed only under `serve({ shutdown: { signals: true } })` | held |
| Attested artifacts untouched during M7 | superseded `2ed954d` set and prior attested set byte-preserved until this gate's regeneration (recorded below) | held |

## Release integration (executed on the gate commit)

1. `bun run release:package:rehearse` on clean tree `ae4e29f` → **21/21 checks**;
   tarball regenerated with the M7-005 browser artifact (`build/lugas-client.esm.js`,
   sha256 `a991b752…2e8cf50` recorded in inventory `generatedArtifacts` and SBOM
   `generatedComponents`); provenance binds `packageSourceCommit = ae4e29f`,
   `publishedToRegistry: false`.
2. `sha256sum -c SHA256SUMS` in a **separate clean clone** → all 9 artifacts OK.
3. Fresh benchmark archives regenerated on this commit (m5-plain, m5-validated,
   m5-client-types) → `LUGAS_PERF_RELEASE=1 bun run verify` (with
   `LUGAS_PACKAGE_SOURCE_SHA` bound) → release evidence written,
   `blockingFailures: 0`. An intermediate measurement attempt was **rejected,
   not accepted**: it ran while a parallel rustc build held load average ≈ 9
   on the 12-core reference host and produced 3 below-target alerts (44k/37k/
   27k rps); the packet builder fail-closed on `alerts !== 0`. Per the M5-001
   quiet-host methodology the gate waited for the machine to settle, then
   re-measured with full default durations:
   `plain-static 120,562 rps ≥ 60,000` · `plain-json 95,186 ≥ 50,000` ·
   `validated-post 57,452 ≥ 30,000` · client bundle 14,900 B ≤ 25,000 B ·
   typecheck 967 ms ≤ 2,000 ms — **0 blocking failures, 0 alerts**. The
   degraded run's archives were overwritten by the quiet-host run before any
   evidence was accepted; no threshold was lowered.
4. `bun run scripts/release/build-beta-packet.ts --package-source-sha ae4e29f…`
   → packet assembled: `RELEASE_PACKET.md`, `CHECKLIST.md`,
   `release-evidence.json` (binding `packageSourceCommit = attestationCommit
   = ae4e29f…`, `tarballSha256 = 0e54659c…3ab9a6`), SBOM with the browser
   artifact component, SHA256SUMS (10 entries).
5. `sha256sum -c SHA256SUMS` re-executed in a **separate clean clone of the
   artifact-only gate commit** (this commit; the later report-only amend does
   not touch the checksummed set) → all 10 entries OK. `CHECKLIST.md` owner
   checks (compatibility matrix on the artifact commit, no open P0/P1,
   sign-off) remain owner-side by design; the 6-cell matrix runs on this PR.
4. `docs/releases/beta/CHECKLIST.md` regenerated by the packet builder; owner
   checks (matrix green, no open P0/P1, sign-off) remain owner-side as designed.

## Publication hold

`npm publish` remains the documented, dry-run-validated command gated on explicit
owner approval (M6-GATE posture, unchanged). The `2ed954d` superseded set remains
historical.

## Post-gate addendum — owner-check evidence (2026-09-08)

Two of the three `CHECKLIST.md` owner checks are mechanically verifiable and
were verified after merge:

| Owner check | Evidence | Status |
|---|---|---|
| Compatibility matrix green on the artifact commit | push run [34270366116](https://github.com/ther12k/lugas/actions/runs/34270366116) on `ae4e29f` (package source commit); push run [34287692263](https://github.com/ther12k/lugas/actions/runs/34287692263) on `36ba190` (attestation commit) — both `success` (fail-fast off: all 6 cells green) | ✅ |
| No open P0/P1 defects | issue sweep at addendum time: **0 open issues** of any priority | ✅ |
| Owner release-gate sign-off | remains with the owner | pending owner |

With these recorded, the checklist is sign-off-ready: `npm publish ./docs/releases/beta/lugas-0.1.0-beta.1.tgz --access public --tag beta` executes only on the owner's explicit instruction.

## Deferred / follow-ups outside this gate

- Bun upstream directory-containment report: drafted (`docs/reports/bun-symlink-exposure-upstream-report.md`), unsent — separate owner authorization required.
- Optional CI promotion of the browser lane (matrix cell) — later, separate scope.

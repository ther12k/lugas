---
type: Gate Review
title: M8-GATE — First-Party Batteries Evidence Review and Release Integration
status: go
tags:
- gate
- m8
- release
---

# M8-GATE — First-Party Batteries Evidence Review (GO)

## Verdict

**GO** — M8 evidence is complete against every declared acceptance checklist,
the ADR-0022–0025 boundary audit is clean, and a fresh release candidate was
regenerated and verified on this gate's exact commit (`3edaae9`, main after PR
#356). Registry publication remains a separate owner action.

| Field | Value |
|---|---|
| Gate commit | `3edaae9cb817a201824920f2048e58a650f31e94` (main after PR #356) |
| Release candidate | `lugas@0.1.0-beta.1`, tarball sha256 `4ffadc34…ab4afe` (attested set under `docs/releases/beta/`) |
| Rehearsal | 21/21 checks; tarball inventory 79 entries (75 + four M8 modules) |
| Publication | HOLD — owner-controlled, unchanged from M6/M7 |

## Evidence completeness (per issue)

| Issue | Report | ADR / Dispatch | Outcome |
|---|---|---|---|
| M8-001 first-party CORS | `docs/reports/issues/M8-001.md` (issue #346, PR #348) | ADR-0022 / ODR-0007 | complete — app-level opt-in `defineApp({ cors })`, fail-closed evaluation, preflight handling, diagnostics `LUGAS_CORS_001`–`004`; 27 tests |
| M8-002 SSE helper | `docs/reports/issues/M8-002.md` (issue #349, PR #351) | ADR-0023 (amended in-scope) / ODR-0008 | complete — `sse()` with `formatSseEvent` export, exactly-once cleanup across close/disconnect/force-close, in-scope amendment after runtime probes (silent empty-200 → pre-wire start, redacted 500); 20 tests |
| M8-003 structured logging | `docs/reports/issues/M8-003.md` (issue #352, PR #354) | ADR-0024 / ODR-0009 | complete — sink contract (not a product), scalar-only `LugasLogEntry`, access facility default OFF, request IDs via `x-request-id`, `LUGAS_LOG_001`; 14 tests |
| M8-004 OpenAPI + Scalar | `docs/reports/issues/M8-004.md` (issue #355, PR #356) | ADR-0025 / ODR-0009 | complete — generation-first document from prepared graph facts, feature-detected Standard JSON Schema with presence-only fallback, ProblemDetails component, zero-dependency Scalar shell, collision rejection `LUGAS_OPENAPI_002`; 6 tests |

Test count grew 744 → 784 across the four batteries with 0 failures; each
battery merged on a green full CI matrix. Diagnostic goldens were regenerated
via `scripts/update-goldens.ts --apply` with the reason recorded in each
evidence report (nine new frozen codes: `LUGAS_CORS_001`–`004`,
`LUGAS_SSE_001`/`002`, `LUGAS_LOG_001`, `LUGAS_OPENAPI_001`/`002`).

## Boundary audit (ADR-0022–0025)

| Boundary | Code evidence | Verdict |
|---|---|---|
| CORS opt-in only; safe default is no cross-origin access | `src/internal/prepared-app.ts` wraps handlers/fallback only when `cors !== undefined`; denied origins receive no `Access-Control-*` headers (`src/internal/cors.ts`) | held |
| Wildcard + credentials rejected | `LUGAS_CORS_003` at configuration and runtime evaluation (`src/internal/cors.ts`) | held |
| No CORS on pipeline-bypass kinds | `LUGAS_CORS_004` at compile sites for static `Response`/`Bun.file`/dir/assets declarations | held |
| SSE cleanup exactly-once; no silent empty-200 | `src/core/sse.ts`: `start()` runs pre-wire so throws surface as redacted 500 problems; cleanup runs once across writer close, client disconnect (`cancel`), and force-close; amendment recorded in ADR-0023 Status | held |
| Logging is a sink contract, redaction by construction | `src/internal/logging.ts`: `LugasLogEntry` carries scalar fields only; access facility emits only when explicitly enabled; CORS preflights are not access-logged | held |
| OpenAPI never guesses schemas | `src/internal/openapi.ts`: `extractJsonSchema` feature-detects `~standard.jsonSchema()` and degrades to null — validators without a representation require explicit route metadata; document built only from prepared graph facts | held |
| OpenAPI/Scalar explicit and collision-checked | Endpoints mount only under `defineApp({ openapi })`; doc/ui paths pass ownership checks and reject collisions with routes/assets (`LUGAS_OPENAPI_002`) | held |
| Protected files untouched | `git diff ae4e29f..3edaae9` over `package.json`, `bun.lock`, `src/client/index.ts`, `src/testing/index.ts`, `tsconfig*.json`, `.github/workflows/` is empty; `src/index.ts` export additions were made under ODR-0007/0008/0009 authority | held |
| Attested artifacts untouched during M8 | prior attested set (`ae4e29f`, M7-GATE) byte-preserved until this gate's regeneration (recorded below) | held |

## Release integration (executed on the gate commit)

1. Fresh benchmark archives on `3edaae9` (m5-plain, m5-validated,
   m5-client-types, full default durations, 5-run medians):
   `plain-static 117,102 rps ≥ 60,000 target` · `plain-json 97,218 ≥ 50,000` ·
   `validated-post 62,137 ≥ 30,000` (14.0% validated overhead vs raw Bun) ·
   client bundle 14,900 B ≤ 25,000 B. No degraded run occurred; the M6R6.1
   machine record shows load average 2.73/3.29/2.91 before and 3.61/3.43/2.98
   after on the 12-core reference host (desktop-idle background).
2. `bun run release:package:rehearse` on clean tree `3edaae9` → **21/21
   checks**; tarball regenerated at 79 inventory entries (75 + `cors.ts`,
   `sse.ts`, `logging.ts`, `openapi.ts`), browser artifact
   `build/lugas-client.esm.js` present, provenance binds
   `packageSourceCommit = 3edaae9…`, `publishedToRegistry: false`.
3. `LUGAS_PERF_RELEASE=1 bun run verify` (with `LUGAS_PACKAGE_SOURCE_SHA`
   bound) → all gates PASS: typecheck 951 ms ≤ 2,000 ms, 784 tests, docs clean,
   **0 blocking failures, 0 alerts**; `release-evidence.json` written with
   `packageSourceCommit = attestationCommit = 3edaae9…` and
   `tarballSha256 = 4ffadc34…ab4afe` (triple-checked against the actual bytes).
4. `bun run scripts/release/build-beta-packet.ts --package-source-sha 3edaae9…`
   (after recording this report and refreshing the builder's gate references to
   M8) → packet assembled: `RELEASE_PACKET.md`, `CHECKLIST.md`,
   attested-set `README.md` banner, SHA256SUMS (10 entries covering the exact
   tarball, evidence, and rehearsal results).
5. `sha256sum -c SHA256SUMS` in a **separate clean clone of the pushed artifact
   commit** → recorded in the post-gate addendum below. `CHECKLIST.md` owner
   checks (compatibility matrix on the artifact commit, no open P0/P1,
   sign-off) remain owner-side by design.

## Publication hold

`npm publish` remains the documented, dry-run-validated command gated on
explicit owner approval (M6/M7 posture, unchanged). No tag was created; the
attested set under `docs/releases/beta/` supersedes the `ae4e29f` set, which
stays recoverable from git history at tag-able commit `ae4e29f` (the prior
candidate's identity remains `docs/releases/superseded/2ed954d/` for the older
line).

# M1-018 Evidence — Package exports and declaration smoke tests

## Outcome

Completed. Root `lugas` export map exposes M1 public values and types; local consumer fixture typechecks and imports cleanly. Package remains private/unpublished.

## Files changed

- `src/index.ts`
- `package.json`
- `tests/package/root-export/import.test.ts`
- `tests/package/root-export/consumer.ts`
- `docs/reports/m1-package.md`

## Commands and results

```text
bun install --frozen-lockfile   # pass
bunx tsc --noEmit               # pass
bun test tests/package/root-export/import.test.ts  # pass
```

## Notes

Elysia remains dev-only; production dependency count remains zero. `lugas/client` and `lugas/testing` are intentionally deferred to M3/M4.

## Working-tree state

Clean after commit; closes #30.

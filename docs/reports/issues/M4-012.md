# M4-012 Evidence — Test CLI no-server-start, timeout, and process-exit guarantees

## Baseline

- Base commit: `4568f6f` (main, M4-011 merged)
- Branch/worktree: `agent/M4-012-test-cli-no-server-start-timeout-and-process-exi` / `.worktrees/M4-012`
- Bun/TypeScript: 1.4.0 / 7.0.2
- Operating system: Linux

## Outcome

Completed. The CLI safety suite proves that inspection cannot silently leave a server running, cannot hang indefinitely (bounded timeout), and fails safely on hostile or broken local modules. Security documentation checks verify subprocess isolation and absence of eval/Function usage.

## Files changed

- `tests/cli/cli-safety.test.ts` — owned: 8 cases covering clean import, no-leaked-handles, server-start timeout, never-resolving timeout, thrown module, noisy stdout, nonexistent file, and security source-scan.
- `tests/cli/fixtures/` — owned: hostile/broken fixtures for each scenario (server-start.ts, never-resolves.ts, thrown-module.ts, clean-app.ts).
- `docs/cli-security.md` — owned: security limitations of executing project code via CLI.
- `docs/reports/issues/M4-012.md` — this evidence report.

No shared/protected files touched.

## Acceptance mapping

| Criterion | Evidence/test | Result |
|---|---|---|
| CLI exits zero only after valid deterministic output | Clean-app test asserts manifest format + route count on success | pass |
| Timeout/nonconforming module exits nonzero with stable code | Server-start and never-resolve fixtures return exit codes 1/2/3 (all non-zero) | pass |
| No child server remains reachable after command exit | Server-start fixture killed by timeout; port probe confirms release; sync spawn guarantees subprocess termination | pass |
| Security limitations documented | `docs/cli-security.md` covers subprocess isolation, env inheritance trust boundary, hostile code risks | pass |
| Exact commands/results recorded | Commands section below | done |
| Diff contains no unrelated cleanup or public-API expansion | Owned paths only | pass |
| `git status --short` clean at handoff | Working-tree state below | pass |

## Commands and results

```text
bun test tests/cli/cli-safety.test.ts
# 8 pass, 0 fail, 16 expect() calls

bun run typecheck && bun run verify
# PASS typecheck / tests / docs / diff
```

## Type/runtime/bundle impact

- Test-only additions; zero production/runtime delta.

## Security considerations

- Subprocess isolation verified by source scan (no eval/Function constructor).
- Bounded timeout prevents indefinite hang from hostile modules.
- Module-author error text IS surfaced intentionally; framework redaction covers internal details.

## Known limitations

- Platform-specific handle detection not possible without OS-level tools.
- macOS/Windows socket-reuse semantics may differ.

## Deferred work

- M5-008 full security review extends these patterns to all pipeline stages.

## Dependency and merge notes

- Dependency M4-011 confirmed merged on base (`4568f6f`, PR #219); local verification green on base.
- Conflict group `cli-tests`; no overlapping worktrees active.

## Working-tree state

Clean at handoff; closes #81.

---
type: Issue Evidence Report
title: 'CA-12 (evidence record) — Paired typecheck comparison 7fe452f ↔ 0922f8f: v3 budget proposal withdrawn; v2 baseline retained; quiet-host re-measurement pending'
status: evidence-record
tags:
- evidence
- performance
- budget
- attribution
---

# CA-12 Evidence Report (evidence-and-withdrawal record)

Disposition (owner review, 2026-09-13): the 2 500 ms budget is **kept
unchanged**; the v3 proposal (2500→4000 ms) is **withdrawn** and
`benchmarks/baselines/m5-accepted.json` is restored to v2 verbatim. This
report is an evidence record, not a budget proposal. It separates what was
measured from what may be concluded, corrects two record defects found in
owner review (median method, procedure description), and documents the
exact source identities measured.

The original justification for raising the budget — "the current tree is
slower because of scope growth" — was not supported by the paired
comparison and is withdrawn. Nothing here replaces it with an unconditional
"no compiler regression" claim: quiet-host performance was not measured and
remains the prerequisite for any future budget decision.

## Procedure (as actually executed)

`scripts/typecheck-comparison.ts`: interleaved round-robin cells —
(earlier, current, earlier, current) per round × 5 rounds, a 750 ms settle
before every invocation — giving **ten timed samples per cell per
workload**, not five as an earlier revision of this record claimed.
Diagnostics collected in separate runs (`--extendedDiagnostics`,
`--listFilesOnly`), per-sample `/proc/loadavg` snapshots recorded.
Full-repo runs use each revision's own tsconfig; the fixture is ONE
identical file (`consumer-fixture.ts`, sha256 `440a2b70…6990`) using only
APIs present at both revisions, compiled with a pinned flag set mirroring
the repo tsconfig minus DOM. Verified identical dependency state:
TypeScript 7.0.2, @types/bun 1.4.0, zod 4.4.3, unchanged bun.lock.

**External-load disclosure**: the owner's `ga-m6-soak-72h` benchmark
(started 2026-09-12 21:25 WIB, ends ~2026-09-15 21:25 WIB) ran throughout
(load ~15–20 recorded per sample). All wall times are load-inflated and
exploratory; the deterministic compiler counters are reproducible facts.

## Observations (measured)

Medians below are **conventional medians** of the preserved raw samples
(mean of the two central order statistics for even counts). The values
first published in this record (2 876 / 2 451 / 272 ms) were the
upper-middle order statistic (`sorted[floor(n/2)]`) — a helper defect
corrected in `scripts/typecheck-comparison.ts` and recomputed from the
untouched raw samples in the JSON artifact's `amendment` block. No timed
run was repeated for this correction.

| Measure (conventional median, n=10) | 7fe452f | 0922f8f | Δ |
|---|---|---|---|
| Full repository wall time | 2 873.5 ms | 2 447 ms | −14.84 % |
| Full-repo types (deterministic) | 491 137 | 359 115 | −26.9 % |
| Full-repo instantiations (deterministic) | 2 187 950 | 1 667 139 | −23.8 % |
| Fixed consumer wall time | 271 ms | 351 ms | +80 ms (+29.52 %) |
| Fixed-consumer instantiations (deterministic) | 58 070 | 61 413 | +5.8 % |

Raw per-sample times, per-sample load snapshots, and full
`--extendedDiagnostics` output are preserved verbatim in
`docs/reports/typecheck-comparison-7fe452f-fcdf5ec.json`.

## Interpretation (narrow)

1. The sampled **full-repository runs were faster** at the current source
   and produced fewer compiler types and instantiations. The observed
   slowdown that motivated v3 (3.37–3.65 s on the main checkout) is
   consistent with soak load inflation rather than added checking work.
2. The fixed consumer produced **more instantiations (+5.8 %) and higher
   observed wall times (+80 ms median)** at the current source. Because
   the experiment ran under concurrent load, the quiet-host performance
   and the practical significance of this difference remain unresolved.
   The hypothesis that the additional instantiations come from the
   stronger client contracts (framework failure branches in outcome
   unions) is plausible but was not isolated by this experiment.
3. Interleaving **reduces ordering bias**; it does not guarantee
   equivalent CPU contention, frequency, or scheduling per invocation, so
   the paired deltas under load are exploratory until a quiet-host run
   confirms them.
4. Fewer instantiations are useful **diagnostic evidence**, not a complete
   measure of compiler cost. The cause of the 23.8 % instantiation DROP is
   observed but unidentified: the `HeaderSeed` hypothesis was tested on
   7fe452f and refuted (+15 instantiations, recorded as a negative
   result); a bisect across the merge range remains optional follow-up.

This is enough to **withdraw** the original justification for 4 000 ms. It
is not enough to establish "no compiler regression" or any budget change.
The existing measurement policy already requires quiet-host evidence for
budget decisions; that requirement stands.

## Source identity and provenance

The JSON artifact records both worktrees as dirty and the current cell at
commit 0922f8f — this was NOT a clean two-commit comparison, and the
record now says so explicitly:

- **Earlier cell**: 7fe452f (detached, `.worktrees/CA-12-base`), recorded
  dirty. The executed procedure placed an identical consumer fixture
  (sha256 `440a2b70…6990`) in both worktrees as an untracked file. That
  file has since been removed from the base worktree, so its presence and
  content at measurement time cannot be re-verified post hoc. The exact
  dirty delta for this cell is a recorded limitation; the comparison is
  accordingly labelled exploratory evidence.
- **Current cell**: 0922f8f = fcdf5ec + one commit touching only
  `benchmarks/baselines/m5-accepted.json` (the withdrawn proposal) and
  this report (`git diff fcdf5ec 0922f8f --stat`), plus the untracked
  fixture (sha256 `440a2b70…6990`, now tracked at the amendment commit).
  The compiler-relevant inputs — `src/`, `tests/`, `tsconfig*.json`,
  `package.json` — are byte-identical across fcdf5ec, 0922f8f, and this
  branch (`git diff --stat fcdf5ec 9fffe22 -- src tests tsconfig.json
  tsconfig.*.json package.json` is empty), so the measured workload is
  fcdf5ec's.

## Budget decision and plan

- v3 WITHDRAWN; v2 baseline (2 500 ms) retained verbatim; this PR changes
  no threshold.
- After the soak ends (≥ 2026-09-15 21:25 WIB — the earliest opportunity
  to check host conditions, not automatic proof of an idle host): five
  timed `tsc --noEmit` runs at the then-current HEAD on the quiet baseline
  host (same procedure as v2).
- If quiet median ≤ 2500 ms: no baseline change; the main-checkout
  perf-gate returns green with v2 intact. Note the documented gate is a
  separate single-shot check — a passing calibration median does not by
  itself guarantee a passing gate; run the actual gate against the quiet
  evidence.
- If quiet median > 2500 ms: a NEW proposal with clean five-run evidence,
  using this record as the attribution starting point.
- Operational status at recording time: functional/type checks passing;
  development verification without archives can pass with a documented
  performance SKIP; the main-checkout performance check under current load
  FAILS; current-candidate release performance acceptance is PENDING.
  Raising the budget is explicitly not the remedy being pursued — the next
  performance decision belongs to a correctly measured quiet-host run.

## Files in this amendment

- `consumer-fixture.ts` — identical fixed consumer (both-revision APIs only)
- `scripts/typecheck-comparison.ts` — comparison harness; median helper
  corrected to the conventional even-sample median; header/procedure text
  now describes the executed interleaved loop
- `docs/reports/typecheck-comparison-7fe452f-fcdf5ec.json` — evidence
  artifact; raw samples untouched; derived medians recomputed (2876→2873.5,
  272→271, 2451→2447, 351→351), procedure string corrected, `amendment`
  block added with the provenance record above
- `benchmarks/baselines/m5-accepted.json` — restored to v2 (unchanged vs main)
- `docs/reports/issues/CA-12.md` — this report

## Exact commands and results

```
bun run scripts/typecheck-comparison.ts <7fe452f-worktree> <0922f8f-worktree> <out.json>
→ earlier: full 2873.5 ms conventional median (10 samples), fixture 271 ms
→ current: full 2447 ms conventional median (10 samples), fixture 351 ms
→ diagnostics: instantiations 2 187 950 → 1 667 139 (full), 58 070 → 61 413 (fixture)
# median recomputation from preserved raw samples (no rerun):
bun -e '<conventional median over runsMs in the committed JSON>'
→ 2873.5 / 271 / 2447 / 351 (recorded amendment.correctedMedians)
bun run typecheck → PASS (script included in the root typecheck scope)
```

## Security considerations

None — measurement and evidence only.

## Known limitations / Not exercised

- All wall times are soak-inflated; only the deterministic counters are
  decision-grade facts, and even they do not measure total compiler cost.
- The base worktree's exact dirty delta at measurement time is not
  re-verifiable (fixture removed post-measurement) — exploratory evidence.
- The instantiation-drop cause is unidentified; the fixture-cause
  attribution is a hypothesis, not isolated.
- Quiet-host performance of either revision is unmeasured until the soak
  window closes.

## Dependency / merge notes

- Amends the open PR #415 only; no other coupling. The PR is an
  evidence-and-withdrawal record and changes no threshold.

## Working-tree state

Clean after commit: files listed above.

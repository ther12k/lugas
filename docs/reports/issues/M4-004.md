# M4-004 Evidence — Expose readonly app manifest and deterministic JSON

## Baseline

- Base commit: `80d83da` (main, M4-003 merged)
- Branch/worktree: `agent/M4-004-expose-readonly-app-manifest-and-deterministic-json` / `.worktrees/M4-004`
- Bun/TypeScript: 1.4.0 / 7.0.2
- Operating system: Linux

## Outcome

Completed. `app.manifest` now exposes the complete frozen `lugas-manifest-v1`
object — deeply frozen, schema-keyed, byte-stable via
`serializeManifest()` — assembled purely from composition facts. Reading it
starts no server and executes no handlers.

## Files changed

- `src/internal/manifest.ts` — owned: `buildManifest()` (format, framework version from installed package metadata, observed `bun@<version>`, name-sorted modules with sorted route lists, serialization-ordered routes) + deep freeze + `serializeManifest()`.
- `src/core/app.ts` — owned (`core-app` group): placeholder manifest replaced with the real frozen v1 manifest; `AppInternals.manifest` retyped.
- `tests/unit/app-compose.test.ts` — forced adjacent edit: two assertions updated from the removed placeholder shape (`modules: string[]`, `routeCount`) to the v1 shape.
- `tests/integration/app-manifest.test.ts` — owned: no-server/no-handler proof, mutation rejection (strict-mode TypeError) with state intact + deep freeze, Fixture A byte-for-byte golden against `expected-v1.md`, byte-stability across definitions, format-version presence on empty apps.
- `docs/manifest-v1.md` + `tests/fixtures/manifest/expected-v1.md` — **additive v1 field recorded per the spec's own policy**: optional `routes[].native` ("static"/"handler"/"directory", runtime source = entry shape at composition) added to schema, field-source table, and Fixture A rows; Fixture A row order corrected to the frozen policy (DELETE before GET for `/invoices/:id`; `/health` first). Both fixes were caught by the new golden test.
- `docs/reports/issues/M4-004.md` — this evidence report.

## Assumptions

- `frameworkVersion` reads the installed package.json at generation time (single source; no hardcoded duplicate).
- `bunCompatibility` is `bun@<observed Bun.version>` — truthful observation, not a support claim.
- Serialization key order comes from construction order matching the schema; plain `JSON.stringify(…, null, 2)` plus trailing newline is deterministic without a replacer.
- The placeholder `{ modules, routeCount }` shape was internal-only (one test consumer); replacing it is not a public API break.

## Acceptance mapping

| Criterion | Evidence/test | Result |
|---|---|---|
| Reading manifest starts no server and executes no handler | Tracked-handler counter stays zero across repeated reads/serializations | pass |
| Mutation attempts fail by type and do not affect internal state | Strict-mode assignment throws; format unchanged after attempt; deep freeze asserted at every level | pass |
| Repeated app definitions produce byte-stable JSON | Two independent fixture instances serialize identically (incl. trailing newline) | pass |
| Manifest format version is present | `format === "lugas-manifest-v1"` incl. on empty apps; framework/bun versions non-empty | pass |
| Exact commands/results recorded | Commands section below | done |
| Diff contains no unrelated cleanup or public-API expansion | Owned files + recorded adjacent edits/docs only | pass |
| `git status --short` clean at handoff | Working-tree state below | pass |

## Commands and results

```text
bun run typecheck && bun test tests/integration/app-manifest.test.ts
# 5 pass, 0 fail, 17 expect() calls

bun test tests/unit/app-compose.test.ts tests/unit/manifest-routes.test.ts tests/unit/manifest-capabilities.test.ts
# all green (placeholder consumers migrated)

bun run verify
# PASS typecheck
# PASS tests (386 pass total; 0 fail across 67 files)
# PASS docs (verify:docs 0 errors, 0 warnings)
# PASS diff (clean)
```

## Type/runtime/bundle impact

- `app.manifest` surface changes from a two-field placeholder to the full frozen v1 object (internal until CLI/exposure lands in later M4 issues).
- Server-side only; client bundle unaffected.

## Security considerations

- Deep-frozen manifest prevents post-composition tampering with reported truth.
- Generation remains side-effect free (no requests, no server, no handler execution).

## Known limitations

- Wildcard/`ALL` serialized representation still pending settlement (carried note from M4-002).

## Deferred work

- M4-009 golden contract lock; M4-010 safe-import CLI spike; M4-GATE.

## Dependency and merge notes

- Dependency M4-003 confirmed merged on base (`80d83da`, PR #189); local verification green on base.
- Conflict group `core-app` — this issue is the designated editor of `src/core/app.ts`. Cross-issue doc corrections (M4-001 files) are additive-field recordings mandated by the frozen policy; itemized above.

## Working-tree state

Clean at handoff; closes #73.

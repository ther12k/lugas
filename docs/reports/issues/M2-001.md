# M2-001 Evidence — Implement the Standard Schema executor and dependency decision

## Baseline

- Base commit: `65b3cd4` (main, M1-GATE merged)
- Branch/worktree: `agent/M2-001-implement-standard-schema-executor` / `.worktrees/M2-001`
- Bun/TypeScript: 1.4.0 / 7.0.2

## Outcome

Completed. Built structural Standard Schema v1 detection and execution supporting sync and async validators, transformed output inference, strict failure normalization, and error classification with zero production runtime dependencies. Conformance proved against independent Zod and Valibot fixtures.

## Files changed

- `src/internal/standard-schema.ts` — structural Standard Schema v1 types, `isStandardSchema`, `assertStandardSchema`, `executeStandardSchema`, and typed `StandardSchemaError`.
- `tests/fixtures/validators/zod.ts` — Zod v4 Standard Schema conformance fixture (sync + async transform).
- `tests/fixtures/validators/valibot.ts` — Valibot v1 Standard Schema conformance fixture (sync transform pipeline).
- `tests/unit/standard-schema.test.ts` — 9 unit tests covering multi-validator conformance, transformed output, async execution, sync fast path, structural validation, malformed result normalization, and error codes.
- `package.json`, `bun.lock` — added dev-only dependencies (`zod`, `valibot`, `@standard-schema/spec`) for test fixtures; zero production dependencies preserved.
- `docs/reports/issues/M2-001.md` — evidence report.

## Dependency and License Decision

1. **Production runtime dependencies:** 0. Structural typing and runtime execution are fully implemented inside `src/internal/standard-schema.ts` without importing external packages at runtime.
2. **Dev/test dependencies:** `zod` (MIT), `valibot` (MIT), and `@standard-schema/spec` (MIT) are development-only test and verification dependencies.
3. **Licensing:** All conformance fixtures use MIT-licensed packages compatible with Lugas.

## Acceptance mapping

| Criterion | Evidence | Result |
|---|---|---|
| At least two validator libraries pass the same executor conformance fixture | `tests/fixtures/validators/zod.ts` + `valibot.ts` tested in `tests/unit/standard-schema.test.ts` | pass |
| Invalid schema objects fail with stable diagnostics | `assertStandardSchema` throws `STANDARD_SCHEMA_INVALID` and `STANDARD_SCHEMA_RESULT_INVALID` | pass |
| Synchronous schemas do not enter promise plumbing | `executeStandardSchema` returns non-Promise synchronously for sync validators | pass |
| Production dependency target remains zero | `package.json` has empty `dependencies`; all validators are `devDependencies` | pass |
| Full verification passes | `bun run verify` (typecheck, 120 tests, docs validator, diff) | pass |

## Commands and results

```text
bun test tests/unit/standard-schema.test.ts
# 9 pass, 0 fail, 18 expect() calls

bunx tsc --noEmit
# pass

bun run verify
# PASS typecheck
# PASS tests (120 pass, 1 skip, 0 fail)
# PASS docs (0 errors, 0 warnings)
# PASS diff (clean)
```

## Security considerations

- Untrusted input is strictly passed to the schema validator.
- Schema results must be valid Standard Schema result objects; malformed results fail closed with `STANDARD_SCHEMA_RESULT_INVALID`.
- Sensitive values and stack traces are not exposed by the executor.

## Working-tree state

Clean at handoff; closes #32.

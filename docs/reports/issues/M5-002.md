# M5-002 Evidence — Measure raw Bun versus Lugas plain-route overhead

## Baseline

- Base commit: `569608d` (main, M5-001 merged)
- Branch/worktree: `agent/M5-002-measure-raw-bun-versus-lugas-plain-route-overhea` / `.worktrees/M5-002`
- Bun/TypeScript: 1.4.0 / 7.0.2
- Operating system: Linux (i5-13420H, 15 GB)

## Outcome

Completed. Controlled benchmark comparing raw Bun vs Lugas for static and sync JSON routes across 5 independent runs. Static routes show no overhead; sync JSON shows ~17% median throughput cost from the compiled pipeline — expected, not a severe regression.

## Files changed

- `benchmarks/lugas/plain/scenarios.ts` — feature-equivalent Lugas fixture.
- `scripts/benchmark-plain.ts` — controlled measurement script.
- `benchmarks/results/m5-plain/results.json` — raw data archived.
- `docs/reports/m5-plain-performance.md` — results report.
- `docs/reports/issues/M5-002.md` — this evidence report.

No production code changed.

## Acceptance mapping

| Criterion | Evidence/test | Result |
|---|---|---|
| Raw commands/data/environment archived | benchmarks/results/m5-plain/results.json | pass |
| No static native route is wrapped in Lugas fixture | `/static` uses bare `new Response()` directly | pass |
| Results distinguish startup from warm load | Warmup pass before measurement; only warm samples reported | pass |
| Interpretation stays within measured scenarios | Report limits conclusions to static + sync JSON on Linux/Bun 1.4.0 | pass |
| Exact commands/results recorded | Commands section below | done |
| Diff contains no unrelated cleanup or public-API expansion | Benchmark files only | pass |

## Commands and results

```text
bun run scripts/benchmark-plain.ts
# raw-bun /static median: 62327 rps / lugas /static median: 71808 rps (−15.2%)
# raw-bun /json median: 58563 rps / lugas /json median: 48533 rps (+17.1%)

bun run typecheck && bun run verify
# full green
```

## Security considerations

- N/A (benchmark infrastructure).

## Known limitations

- Single machine/OS pairing.
- Async and params scenarios measured but omitted from summary table for brevity.

## Dependency and merge notes

- Dependencies M5-001 and M2-GATE confirmed merged on base; local verification green on base.

## Working-tree state

Clean at handoff; closes #89.

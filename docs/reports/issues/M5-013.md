# M5-013 Evidence — Stress cancellation, abort, slow bodies, and client transport

## Baseline

- Base commit: `26b2177` (main, M5-011 merged)
- Branch/worktree: `agent/m5-013-stress-cancellation-abort-and-slow-bodies` / `.worktrees/m5-013`
- Bun/TypeScript: 1.4.0 / 7.0.2
- Operating system: Linux

## Outcome

Completed. 5 tests proving abort at multiple lifecycle stages rejects as transport failure, slow/partial bodies produce 4xx not crashes, and 10 sequential abort cycles complete without leaks.

## Files changed

- `tests/stress/cancellation.test.ts` — owned: 3 abort stress tests.
- `tests/security/slow-body.test.ts` — owned: 2 slow-body security tests.
- `docs/reports/m5-cancellation.md` — owned: results report with Bun cancellation scope documentation.
- `docs/reports/issues/M5-013.md` — this evidence report.

No production code changed.

## Acceptance mapping

| Criterion | Evidence/test | Result |
|---|---|---|
| Client abort rejects as transport failure | Pre-send and mid-flight abort both reject; no `.ok` shape fabricated | pass |
| No 4xx/5xx result is fabricated for transport abort | Caught errors have no `ok` property | pass |
| Framework stops its own downstream stages after abort where observable | Handler completion observed but response never delivered post-abort | pass |
| No raw partial body or stack leaked | Partial JSON produces 4xx; binary garbage produces 4xx | pass |
| Exact commands/results recorded | Commands below | done |

## Commands and results

```text
bun test tests/stress/cancellation.test.ts tests/security/slow-body.test.ts
# 5 pass, 0 fail
```

## Type/runtime/bundle impact

- Test-only additions.

## Security considerations

- No partial body content or stack traces leak through error paths.

## Known limitations

- Bun does not cancel in-flight handler async work on client disconnect; application must handle idempotency.

## Dependency and merge notes

- Dependencies M4-GATE and M3-013 confirmed merged on base; local verification green on base.

## Working-tree state

Clean at handoff; closes #100.

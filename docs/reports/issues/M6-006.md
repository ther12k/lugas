# M6-006 Evidence — Finalize the supported Bun 1.4 compatibility matrix

## Baseline
- Base commit: `5324aee` (main after M6R2 runtime-boundary; beta API candidate)
- Issue: #111 (M6-006)
- Bun/TypeScript/npm: 1.4.0 / 7.0.2 / 11.6.1, linux-x64

## Outcome

Completed. The compatibility matrix is finalized from verified source data:

- **`docs/compatibility.md`** (owned): user-facing table of exact supported
  combinations — Bun **1.4.0** and current **1.4.x patch** × Linux x86-64 /
  macOS arm64 / Windows x64 for server core+CLI+testing; TypeScript **7.0.2**
  pinned; Standard Schema v1 validators (zod ^4.4.3, valibot ^1.4.2, spec
  contract). Unsupported combos are explicit non-goals: Node server core,
  Bun outside 1.4.x, real-browser execution beyond bundle-level proof.
- **`docs/reports/m6-compatibility.md`** (owned): matrix evidence distinguishing
  pre-PR candidate run 33000006619 (`5324aee`) from the authoritative
  merged-tree run **33021193847 (`d9cfd08`, includes the verifier and per-cell
  CLI tests)** — all 6 cells green in both; skip/failure disposition; badge URLs.
  NOTE: hardening of this evidence packet happened post-closure under #294;
  both documents were updated there (run disambiguation, structured verifier,
  lock-resolved versions, CLI matrix coverage).
- **Canonical command**: `bun run verify:compatibility-report`
  (`scripts/verify-compatibility-report.ts`) mechanically guards
  doc-vs-reality drift: workflow-matrix ↔ doc-table consistency, no broad
  semver Bun claims, toolchain pins match package.json. Wired into
  `.github/workflows/compatibility.yml` so every future matrix run re-proves
  the published table (protected shared file change recorded here per AGENTS.md).
- **Perf-gate chronology** (fail-closed behavior observed honestly):
  - Attempt 1 — FAIL: benchmark archive generated while unrelated heavy builds
    ran on this shared host; gate rejected contaminated numbers (3 blocking).
  - Attempt 2 — FAIL: cleaner but still-contended host; validated-post 29908 rps
    just below its 30000 target and typecheck measurement 4744ms > 2000ms budget.
  - Attempt 3 — PASS on quiet host, accepted as final evidence:
    plain-static 106883 rps ≥ 60000 · plain-json 93025 rps ≥ 50000 ·
    validated-post 51683 rps ≥ 30000 · typecheck 1116ms ≤ 2000ms (commit `5324aee` worktree,
    bun.lock clean install).

## Files changed
- `docs/compatibility.md` — owned (new).
- `docs/reports/m6-compatibility.md` — owned (new).
- `scripts/verify-compatibility-report.ts` — owned (new): canonical verifier.
- `package.json` — protected: adds `verify:compatibility-report` script only.
- `.github/workflows/compatibility.yml` — protected: one verifier step added.
- `docs/reports/issues/M6-006.md` — this report.

## Acceptance mapping

| Criterion | Evidence | Result |
|---|---|---|
| Every supported combination has passing evidence | CI run 33000006619: 6/6 cells success on candidate commit | pass |
| Unsupported combos explicit | "Explicit non-goals" section in docs/compatibility.md | pass |
| Latest compatible 1.4 patch tested near release | `1.4.x` cell tracks latest patch (ubuntu/macos/windows green); GA rerun in M6-009 | pass |
| Matrix generated/verified from source data | verifier enforces workflow↔doc consistency; runs in every matrix job now | pass |
| Skips/failures resolved or documented | disposition table: 1 intentional skip w/ rationale link; zero failures | pass |
| User-facing table + badge data | docs/compatibility.md + badge URLs in m6-compatibility.md | pass |

## Exact commands and results
```text
bun run verify:compatibility-report   # PASS (10 checks green)
bun test                              # 605 pass, 0 fail, 1 documented skip
bun run verify                        # exit 0 — incl. real-evidence perf gate:
#   ✓ plain-static 106883 ≥ 60000 · plain-json 93025 ≥ 50000
#   ✓ validated-post 51683 ≥ 30000 · typecheck 1116ms ≤ 2000ms
gh run view 33000006619               # 6 jobs → success
```

## Security considerations
None — documentation/tooling only.

## Known limitations / deferred
Matrix cells run the portable subset (typecheck/unit/conformance/security);
deepest suites remain Linux-local until post-beta. Final release-date rerun
belongs to M6-009.

## Working-tree state
Clean at handoff.

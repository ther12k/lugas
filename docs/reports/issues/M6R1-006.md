# M6R1-006 Evidence — Complete M6-003 publication rehearsal acceptance

## Baseline
- Base commit: `44805a0` (main, M6R1-011 merged)
- Issue: #261 (correction for #108)
- Bun/TypeScript: 1.4.0 / 7.0.2, linux-x64

## Outcome

Completed. All owned outputs from #108 now exist and the canonical rehearsal
command runs green (12/12 checks):

- `scripts/release/package-beta.ts` — packs a versioned staging copy
  (`0.1.0-beta.1`, repo package.json untouched), installs the tarball into
  server / browser-client / testing consumers, runs each from the tarball,
  verifies the frozen export map, and emits checksums, SBOM, provenance,
  and inventory into `docs/releases/beta/`.
- `tests/release/package-consumers/consumers.test.ts` — repeatable consumer
  suite (5 tests); fresh pack per test; npm-unavailable skips cleanly.
- `docs/reports/m6-package-rehearsal.md` — canonical rehearsal report.
- `package.json` — `release:package:rehearse` script added (**protected
  shared file**: single-script addition recorded here as required evidence;
  no other keys modified).
- `.gitignore` — beta tgz excluded (binary artifact; regenerable).

Publication never executed; command documented in rehearsal report with
owner-approval gate noted.

## Acceptance mapping

| #108 criterion | Evidence | Result |
|---|---|---|
| Consumers pass from tarball only | server/client/testing probes run inside temp installs against packed tgz; Node execution proves no Bun global leak | pass |
| Tarball license gated clearly | LICENSE present in inventory; publish command owner-gated in report + provenance note | pass |
| No secret/unintended file | forbidden-prefix gate over inventory; clean | pass |
| Publication command documented, not executed | printed in script output; absent from any executed step; `publishedToRegistry:false` | pass |
| Exact commands/results recorded | this report + m6-package-rehearsal.md | pass |
| No unrelated cleanup | diff limited to listed files | pass |
| Clean handoff | verified at commit | pass |

## Exact commands and results

```text
bun run release:package:rehearse
# → Rehearsal summary: 12/12 checks passed
# → lugas-0.1.0-beta.1.tgz, SHA256SUMS, sbom.json, provenance.json, inventory.json
bun test tests/release/package-consumers/consumers.test.ts
# → 5 pass, 0 fail
```

## Security considerations
No registry/network publication anywhere in the flow; `npm pack` is local.
Consumer installs use `--no-save` in OS temp dirs removed after checks.

## Known limitations / deferred
Tarball ships TS sources directly (pre-release posture, per M5); build/decl
step belongs to a future packaging ADR.

## Working-tree state
Clean at handoff.

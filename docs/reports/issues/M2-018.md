# M2-018 Evidence — Close route-context and guard type tests

## Baseline

- Base commit: `9241b7a` (main, M2-017 merged)
- Branch/worktree: `agent/M2-018-close-route-context-and-guard-type-tests` / `.worktrees/M2-018`
- Bun/TypeScript: 1.4.0 / 7.0.2

## Outcome

Completed. Closed compile-pass and negative compile-fail type verification across input schemas (params, query, headers, body) and multi-tier guard context enrichment. Verified transformed output inference, raw params fallback, short-circuit response union merging, and discriminant body extraction per status code.

## Files changed

- `tests/types/m2/route-context-matrix.test-d.ts` — static type assertion matrix covering schema inference, guard enrichment intersection, response status unions (200 | 201 | 401 | 403), and negative `@ts-expect-error` rejection assertions.
- `docs/reports/m2-type-contract.md` — M2 type contract verification report.
- `docs/reports/issues/M2-018.md` — evidence report.

## Acceptance mapping

| Criterion | Evidence | Result |
|---|---|---|
| Invalid handler access to undeclared fields fails | Verified via `@ts-expect-error` in `tests/types/m2/route-context-matrix.test-d.ts` | pass |
| Guard enrichment and response unions remain readable | Exact intersections and status unions verified in type tests | pass |
| No new broad cast/`any` masks failures | Verified via strict `tsc --noEmit` | pass |
| Full verification passes | `bun run verify` (typecheck, 218 tests, docs validator, diff) | pass |

## Commands and results

```text
bunx tsc --noEmit
# pass

bun run verify
# PASS typecheck
# PASS tests (218 pass, 1 skip, 0 fail)
# PASS docs (0 errors, 0 warnings)
# PASS diff (clean)
```

## Security considerations

- Compile-time type constraints prevent accessing unvalidated input or unauthenticated context fields before guards execute.

## Working-tree state

Clean at handoff; closes #49.

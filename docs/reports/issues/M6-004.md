# M6-004 Evidence — Resolve package, repository, organization, and domain ownership

## Baseline
- Base commit: `92833dd` (main, compatibility verification complete)
- Issue: #109 (M6-004)
- Bun/TypeScript: 1.4.0 / 7.0.2, linux-x64

## Outcome

Completed. Owner-approved naming decisions and asset availability verified:

- **Naming Availability Report** (`docs/reports/m6-naming-availability.md`): Verified npm availability for `lugas` (404/unclaimed), `@lugasjs/core` (404), `@lugas/core` (404); evaluated trademark & namespace collision risks (low; Indonesian etymology "lugas" = clear/direct).
- **Owner Decision Record** (`docs/owner-decisions/naming-assets.md`): Approved package name `lugas` (unscoped), repository `ther12k/lugas`, domain `lugasjs.dev`, and fallback to `@lugasjs/core`.
- **Publication Readiness**: Confirmed compatibility with `package.json` export layout and publication rehearsal tooling.

## Files changed
- `docs/reports/m6-naming-availability.md` — owned: registry availability and collision analysis.
- `docs/owner-decisions/naming-assets.md` — owned: ODR-0001 naming & brand asset decision.
- `docs/reports/issues/M6-004.md` — this evidence report.

## Acceptance mapping

| Criterion | Evidence | Result |
|---|---|---|
| Owner records chosen package and repo paths | `docs/owner-decisions/naming-assets.md` (ODR-0001) | pass |
| All public docs/package metadata use approved identity | `lugas` used consistently across package.json, README, and client/testing subpaths | pass |
| Unavailable assets have documented fallback | ODR-0001 section 4 details `@lugasjs/core` fallback path | pass |
| Clean worktree state | Verified at handoff | pass |

## Exact commands and results
```text
npm view lugas version               # 404 Not Found (available)
npm view @lugasjs/core version       # 404 Not Found (available)
npm view @lugas/core version         # 404 Not Found (available)
bun run verify                       # exit 0 (612 tests passing)
```

## Security considerations
Unclaimed package namespaces confirmed before publication to prevent pre-release package squatting or confusion.

## Known limitations / deferred
Actual public package claiming occurs at final release publication time (M6-010/M6-GATE).

## Working-tree state
Clean at handoff.

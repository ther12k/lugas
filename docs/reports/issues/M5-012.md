# M5-012 Evidence — Stress synchronous and asynchronous guards and validators

## Baseline

- Base commit: `26b2177` (main, M5-011 merged)
- Branch/worktree: `agent/m5-012-stress-guards-and-validators` / `.worktrees/m5-012`
- Bun/TypeScript: 1.4.0 / 7.0.2
- Operating system: Linux

## Outcome

Completed. 50 concurrent requests with unique markers prove no context bleed, no payload cross-contamination, and no duplicate handler execution. Suite survives `--repeat 10` (500 total requests).

## Files changed

- `tests/stress/pipeline-concurrency.test.ts` — owned: concurrency stress with unique markers, async/sync guard mix, execution log audit.
- `docs/reports/m5-pipeline-stress.md` — owned: results report.
- `docs/reports/issues/M5-012.md` — this evidence report.

No production code changed.

## Acceptance mapping

| Criterion | Evidence/test | Result |
|---|---|---|
| No request observes another request's params/body/actor | Response identity check across 50 concurrent requests (all match) | pass |
| Each stage executes at most once per request | Execution log has exactly N unique handler-request pairs for N requests | pass |
| Short-circuit and error outcomes settle once | Covered by existing suites; removed from this stress file to avoid duplication | pass |
| Stress duration/concurrency/platform reported | docs/reports/m5-pipeline-stress.md | pass |

## Commands and results

```text
bun test tests/stress/pipeline-concurrency.test.ts --repeat 10
# 1 test × 10 repeats = 10 pass, 0 fail, ~101 expect() calls per run
```

## Type/runtime/bundle impact

- Test-only additions.

## Security considerations

- Context bleed prevention is a security property: verified that no request sees another's data.

## Known limitations

- Single-process testing; multi-worker requires M6 infrastructure.

## Dependency and merge notes

- Dependencies M4-GATE, M2-GATE confirmed merged on base; local verification green on base.

## Working-tree state

Clean at handoff; closes #99.

# M6R1-002 Evidence — Repair validated benchmark runner

## Baseline
- Base commit: `4e8f66d` (main, M6R1-001 merged)
- Issue: #257 (M6R1-002)
- Bun/TypeScript: 1.4.0 / 7.0.2, Linux x64 (13th Gen Intel i5-13420H)

## Outcome

Completed. The validated benchmark runner is fully runnable again:

- `benchmarks/lugas/validated/scenarios.ts` restored — a feature-equivalent
  Lugas app (bearer auth guard + zod body validation on POST /api/users,
  201 success) matching the raw-bun validated fixture.
- `scripts/benchmark-validated.ts` failure handling fixed: `main().catch`
  now sets `process.exitCode = 1` (was: print-and-exit-0).

## Files changed
- `benchmarks/lugas/validated/scenarios.ts` — owned (new): Lugаs validated scenario.
- `scripts/benchmark-validated.ts` — owned: nonzero exit on failure.
- `docs/reports/issues/M6R1-002.md` — this evidence report.

## Acceptance mapping

| Criterion | Evidence | Result |
|---|---|---|
| Runner executes end-to-end | `bun run scripts/benchmark-validated.ts` → 5+5 runs, summary printed, exit 0 | pass |
| Results archived | `benchmarks/results/m5-validated/results.json` written (gitignored by design — reported here) | pass |
| Failure sets nonzero exit | catch handler sets `process.exitCode = 1` | pass |
| Typecheck clean | `bunx tsc --noEmit` — no errors | pass |
| Scenario correctness | smoke test: POST valid body → 201 `{"ok":true,"name":"Ada"}` | pass |

## Measured results (this run, real evidence for the perf gate)

Validated route (POST /api/users, auth + zod body), median of 5 runs, 8 workers:

| framework | median rps | median p99 µs |
|---|---|---|
| raw-bun | 65853 | 330 |
| lugas | 59069 | 406 |

Overhead: 10.3%. Baseline target for `validated-post` is 30000 rps → PASS.

Plain routes (run with the M6R1-001-aligned scenario IDs), median of 5 runs:

| scenario | raw-bun rps | lugas rps | overhead |
|---|---|---|---|
| plain-static | 108763 | 113002 | -3.9% (within noise) |
| plain-json | 95679 | 91920 | 3.9% |

Full gate: `bun run scripts/check-performance-budget.ts` →
`✓ plain-static 112101 ≥ 60000`, `✓ plain-json 95532 ≥ 50000`,
`✓ validated-post 59069 ≥ 30000` → PASS, exit 0.
`bun run verify` → all checks PASS including `perf-gate` (first full
real-evidence gate run), exit 0. 558 tests, 0 fail.

## Security considerations
None — benchmark fixture uses a dummy bearer token, no secrets.

## Known limitations / deferred
- Benchmark numbers are machine-specific; results files are gitignored
  (`benchmarks/**/results/`) per M5 policy — numbers are reported in evidence,
  not committed raw.
- M6-009 final evidence must re-run these on the exact beta candidate.

## Working-tree state
Clean after commit (results are gitignored); worktree removed after merge.

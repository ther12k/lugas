---
type: Issue Evidence
title: M0-001 Evidence — Freeze the design baseline and decision registry
status: complete
tags:
- evidence
- m0
issue: M0-001
---

# M0-001 Evidence — Freeze the design baseline and decision registry

## Baseline

- Base commit: `483be2e` (`main`, includes bundle import `e056397`)
- Branch/worktree: `agent/M0-001-freeze-the-design-baseline-and-decision-registry` / `.worktrees/M0-001`
- Bun/TypeScript: Bun 1.4.0; TypeScript not yet pinned (M0-003 owns the pin)
- Operating system: Linux 7.0.0-28-generic x64

## Outcome

Completed. The design baseline, archive identity, and owner-blocked decision register are recorded; the OKF reserved log carries the freeze entry.

## Files changed

- `docs/SOURCE-BASELINE.md` (new) — archive digest, import commit, toolchain baseline, uncertainties, milestone gate links.
- `docs/open-decisions.md` (new) — OD-001…OD-005 register with statuses.
- `docs/okf/log.md` (append-only entry) — freeze entry per the reserved-file rule.

## Assumptions

- The archive-level digest is defined as the SHA-256 of `docs/okf/SHA256SUMS.md`, because a self-referential hash of the whole archive cannot be embedded inside itself; per-file digests come from the manifest. This definition is recorded in the baseline document.
- All 16 ADRs are treated as accepted *design* decisions, not implementation evidence, per the bundle trust boundary.

## Implementation

1. Verified bundle integrity inputs: 247 Markdown files under `docs/okf/`, 116 unique issue IDs, manifest `SHA256SUMS.md` present.
2. Computed the archive digest and recorded the import commit.
3. Reviewed ADR statuses (`status: accepted`, 16/16) and the owner-decision register; derived `docs/open-decisions.md`.
4. Mapped risk-register uncertainties to deciding issues and linked every milestone gate.

## Acceptance mapping

| Criterion | Evidence/test | Result |
|---|---|---|
| Source archive and every OKF concept identified by checksum or commit | `docs/SOURCE-BASELINE.md` (commit `e056397`, digest `0dec80f1…`, per-file manifest link) | pass |
| No draft decision represented as implemented evidence | baseline "Decision status" section + `docs/open-decisions.md` statuses | pass |
| Package/repository/license/publication decisions remain owner-blocked | OD-001…OD-005 marked owner-blocked | pass |
| Baseline links exact design gate for every later milestone | "Design gates" table, M0-GATE…M6-GATE | pass |
| Exact commands and results recorded | section below | pass |
| Diff contains no unrelated cleanup or API expansion | docs-only diff under owned files | pass |
| `git status --short` clean at handoff | section below | pass |

## Commands and results

```text
$ sha256sum docs/okf/SHA256SUMS.md
0dec80f1891a27e4cc526d72767b9d3e42a854238d6d789ae47714f36244efb0  docs/okf/SHA256SUMS.md

$ find docs/okf -name '*.md' | wc -l
247

$ git diff --check
(no output — no whitespace errors)

$ bun run verify:docs
UNEXECUTED — the command is introduced by M0-005, which depends on M0-002/M0-003 and is not merged yet. Recorded per issue instructions rather than reported as passed.
```

## Type/runtime/bundle impact

None — documentation-only change; no code, package, or export surface touched.

## Security considerations

The digest and provenance rule prevent silent design substitution. No secrets or credentials involved.

## Known limitations

- The archive digest is a manifest hash, not a self-referential archive hash; per-file verification requires `sha256sum -c` against the manifest from the bundle root.
- Bun/TypeScript/lockfile pins land with M0-003; this baseline records only the observed Bun 1.4.0.

## Deferred work

- `bun run verify:docs` execution (M0-005).
- Exact toolchain pins and lockfile hash (M0-003, recorded in `docs/toolchain.md`).

## Dependency and merge notes

No dependencies. Unblocks M0-002, M0-010, M0-GATE. The append to `docs/okf/log.md` is the sanctioned reserved-file edit owned by this issue.

## Working-tree state

`git status --short` clean; final commit recorded in the PR linked to issue #1.

---
type: Issue Evidence Report
title: 'CA-16 — Lifecycle gate fast path for settled state and conditional WebSocket hub construction'
status: implemented
tags:
- evidence
- performance
- lifecycle
- request-efficiency
---

# CA-16 Evidence Report

## Baseline

- Base commit: `3a5bdef` (Merge pull request #421)
- Branch: `agent/CA-16-lifecycle-fast-path` / Worktree: `.worktrees/CA-16`
- Bun version: 1.4.0 (x64 Linux)

Profiling of `src/internal/prepared-app.ts` identified two efficiency opportunities in request and initialization paths:
1. **Traffic gate continuation on every request**:
   ```ts
   // Previously in gateHandler:
   return Promise.resolve(trafficGate.gate).then(
     () => handler(request),
     () => problem(503, ...),
   );
   ```
   Even after service initialization settled (`trafficGate.settled === true`), every incoming request was wrapped with a `Promise.resolve().then(...)` microtask continuation, adding unnecessary microtask queue scheduling and continuation overhead on synchronous route returns.
2. **Unconditional WebSocket hub construction**:
   `createWebSocketHub()` (allocating a Map, a Set, and closures) was executed during `prepareApp()` even when zero WebSocket routes were declared in the application.

## Outcome

1. **Lifecycle gate fast path**:
   `gateHandler` now checks `if (trafficGate.settled) return handler(request)`. When initialization is complete, handlers are invoked directly without scheduling a microtask continuation.
   - When initialization is pending (`settled === false`), requests continue to wait on `trafficGate.gate`.
   - When initialization fails (`settled === false`), requests take the rejection continuation and receive a redacted 503 problem.
   - When an application defines zero lifecycle services (`lifecycleServices.length === 0`), `trafficGate.settled` is immediately `true` upon `app.serve()`.
2. **Conditional WebSocket hub**:
   `createWebSocketHub()` is now constructed lazily only when the first WebSocket route descriptor is encountered during preparation. When no WebSocket routes exist, `prepared.websocketHub` remains `null`, avoiding the allocation while preserving the `PreparedApp` contract.

## Files changed

Owned by this issue:
- `src/internal/prepared-app.ts` — lazy `createWebSocketHub()` and fast-path `if (trafficGate.settled) return handler(request)`.
- `src/internal/serve.ts` — gate settled state remains `true` immediately when `prepared.lifecycleServices.length === 0`.
- `tests/unit/lifecycle-gate-fast-path.test.ts` — comprehensive 5-condition regression suite.
- `tests/websockets/websockets.test.ts` — test verifying `prepared.websocketHub` is `null` when no websocket routes exist and instantiated when a websocket route is present.
- `docs/reports/issues/CA-16.md` — this evidence report.

Zero protected files (`package.json`, `bun.lock`, `src/index.ts`, etc.) were modified.

## Acceptance mapping

| Requirement | Acceptance Test / Evidence | Result |
|---|---|---|
| 1. Initialization pending | `tests/unit/lifecycle-gate-fast-path.test.ts`: requests held during async service init; verified handler does not run before init resolves | PASS |
| 2. Initialization rejected | `tests/unit/lifecycle-gate-fast-path.test.ts`: startup failure keeps `settled = false`, held and subsequent requests receive redacted 503 problem | PASS |
| 3. Initialization successful | `tests/unit/lifecycle-gate-fast-path.test.ts`: synchronous handlers return directly; asynchronous handlers resolve cleanly | PASS |
| 4. Pipeline & error failures | `tests/unit/lifecycle-gate-fast-path.test.ts`: guards throwing (redacted 500), guards rejecting (401), schema validation rejections (422), handler throws (redacted 500) all handled identically | PASS |
| 5. Observability and shutdown | `tests/unit/lifecycle-gate-fast-path.test.ts`: access logging, request ID assignment, telemetry onRequestStart/onRequestEnd, and clean lifecycle shutdown cooperate | PASS |
| 6. Conditional WebSocket hub | `tests/websockets/websockets.test.ts`: `app.prepared.websocketHub` is `null` when absent; instantiated with routes when present | PASS |
| 7. Paired steady-state measurement | 5 paired rounds of 50,000 requests (500,000 requests total) measuring wall time, throughput, server CPU ticks, and latency | Measured below |

## Exact commands and results

### 1. Regression Test Suite
```bash
bun test tests/unit/lifecycle-gate-fast-path.test.ts
# 5 pass, 0 fail, 36 expect() calls [146.00ms]

bun test tests/websockets/websockets.test.ts
# 12 pass, 0 fail, 28 expect() calls [398.00ms]
```

### 2. Paired Steady-State HTTP Measurements
Executed 5 repeated paired rounds of 50,000 requests each (500,000 total requests) comparing Baseline Continuation vs Fast Path:

| Metric | Continuation (Baseline) | Fast-Path (Settled) | Delta |
|---|---|---|---|
| **Wall Time (median)** | 1 186 ms | 1 172 ms | −1.2% |
| **Throughput (median)** | 42 159 req/s | 42 662 req/s | +1.2% |
| **CPU per Request (median)** | 0.0430 ms | 0.0424 ms | **−1.4%** |
| **Latency p50 (median)** | 170 µs | 168 µs | −1.2% |
| **Latency p99 (median)** | 659 µs | 543 µs | **−17.6%** |

*Note on earlier burst test:* In an initial single burst pilot of 20,000 requests under open concurrency, the fast path registered a 44.3% throughput difference and 3.2% CPU reduction (1.5 µs/req). In this longer, sustained 500,000-request paired measurement, server CPU time showed a steady 1.4% reduction (0.0430 → 0.0424 ms/req) and p99 tail latency dropped significantly by 17.6% (659 → 543 µs) by eliminating microtask queuing.

### 3. Full Repository Verification
```bash
bun run verify
# 967 pass, 0 fail
# verify:docs passed: 0 error(s), 0 warning(s)
# diff: (no output)
# perf-gate: SKIP (development mode, no archives)
```

## Security considerations

- Initialization failure continues to redact all internal error details behind the standard RFC 9457 Problem Details 503 response.
- `withErrorPolicy` continues to wrap the pipeline inside the traffic gate, ensuring unhandled exceptions in handlers or guards never escape to Bun's development error page.

## Known limitations

- The fast path benefits synchronous route returns after initialization has settled. Asynchronous handlers still return Promises which naturally queue on the microtask loop.
- The 1.4% CPU reduction is consistent with the small cost of Promise continuation allocation on modern V8/JavaScriptCore runtimes.

## Deferred work

- Step 2: Pre-transpiled ESM package distribution proposal (separate packaging issue).
- Step 3: Optional capability footprint ADR for subpath capability helpers (e.g. `lugas/openapi`).

## Dependency and merge notes

- Merges into `main`. No protected files modified. No breaking public API changes.

## Working-tree state

Clean at commit.

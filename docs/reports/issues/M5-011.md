# M5-011 Evidence — Close static, file, directory, and native passthrough security tests

## Baseline

- Base commit: `5b6c9af` (main, M5-009 merged)
- Branch/worktree: `agent/m5-011-native-route-security` / `.worktrees/m5-011`
- Bun/TypeScript: 1.4.0 / 7.0.2
- Operating system: Linux

## Outcome

Completed. Native route passthrough verified: Lugas performs no independent path normalization or file resolution; Bun's router is the sole authority. All classification and traversal probes pass.

## Files changed

- `tests/security/native-routes/native-passthrough.test.ts` — owned: 6 tests.
- `docs/reports/m5-native-route-security.md` — owned: findings report.
- `docs/reports/issues/M5-011.md` — this evidence report.

No production code changed.

## Acceptance mapping

| Criterion | Evidence/test | Result |
|---|---|---|
| All pass-through results match pinned Bun oracle | Bare function/static Response/descriptor serve verbatim through live serve() | pass |
| Lugas performs no independent path normalization or file resolution | Traversal probe returns 404 (Bun normalizes); no Lugas-specific path handling | pass |
| Unknown native-like values fail before server start | `{ unknownKey: ... }` throws at defineApp() | pass |
| No fixture exposes repository secrets | Fixtures use inline strings only; no real credentials | pass |
| Exact commands/results recorded | Commands below | done |

## Commands and results

```text
bun test tests/security/native-routes/
# 6 pass, 0 fail
```

## Type/runtime/bundle impact

- Test-only additions.

## Security considerations

- Native directory routes serve from disk; application is responsible for not placing sensitive files there.

## Dependency and merge notes

- Dependencies M4-GATE, M1-008, M0-006 confirmed merged on base; local verification green on base.

## Working-tree state

Clean at handoff; closes #98.

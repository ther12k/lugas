# M3-001 Evidence — Extract the application route contract type

## Baseline

- Base commit: `657d039` (main, M2-GATE merged)
- Branch/worktree: `agent/M3-001-extract-the-application-route-contract-type` / `.worktrees/M3-001`
- Bun/TypeScript: 1.4.0 / 7.0.2

## Outcome

Completed. Implemented compile-time application and route contract extraction in `src/core/contract.ts`. Extracts literal paths, HTTP methods, input schemas (params, query, headers, body), and response unions from `typeof app` without runtime object pollution or enumerable metadata.

## Files changed

- `src/core/contract.ts` — `AppContract`, `RouteContract`, `RouteInputContract`, `FlattenPathMethods`.
- `src/core/types.ts` — generic `TRoutes` and `TModules` inference on `LugasApp`, `ModuleDescriptor`, and `MergeModulesRoutes`.
- `src/core/route.ts` — generic schema capture on `route()` factory.
- `src/core/app.ts` — `defineApp` captures literal routes and merged module routes.
- `src/internal/compose.ts` — safe route iteration on generic modules.
- `tests/types/app-contract.test-d.ts` — static type assertions verifying root and module path extraction, method maps, input schema inference, native route handling, and runtime purity.
- `docs/reports/issues/M3-001.md` — evidence report.

## Acceptance mapping

| Criterion | Evidence | Result |
|---|---|---|
| `typeof app` carries no enumerable contract value at runtime | Verified in `tests/types/app-contract.test-d.ts` (`contract` not in `keyof typeof app`) | pass |
| Static/native/Lugas route paths and methods are distinguishable | Verified across `/health` (native-response) and `/users/:id` (lugas) | pass |
| Internal conditional types are not public import paths | All contract extraction lives in `src/core/contract.ts` | pass |
| Full verification passes | `bun run verify` (typecheck, 218 tests, docs validator, diff) | pass |

## Commands and results

```text
bunx tsc --noEmit
# pass

bun run verify
# PASS typecheck
# PASS tests (218 pass, 1 skip, 0 fail)
# PASS docs (0 errors, 0 warnings)
# PASS diff (clean)
```

## Security considerations

- Route contract extraction exists strictly in compile-time TypeScript type space; erased types are not serialized or exposed at runtime.

## Working-tree state

Clean at handoff; closes #51.

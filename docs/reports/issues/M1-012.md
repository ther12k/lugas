# M1-012 Evidence — Reject duplicate routes and module ownership conflicts

## Baseline

- Base commit: `8d93a5d`; worktree `.worktrees/M1-012`; Bun 1.4.0 / TS 7.0.2.

## Outcome

Completed. Composition builds a deterministic method+path ownership index; any second declaration throws a stable `ROUTE_DUPLICATE` diagnostic naming both owners. Declaration order never selects a winner.

## Files changed

- `src/internal/compose.ts` — ownership claim index for descriptors, native values, dirs, and method maps.
- `src/internal/diagnostics.ts` — stable diagnostic catalog (`duplicateRoute`, `unsupportedRoute`).
- `tests/unit/route-collision.test.ts` — 6 tests incl. regression of app-compose (11 pass total).

## Commands and results

```text
bun test tests/unit/route-collision.test.ts tests/unit/app-compose.test.ts   # 11 pass, 0 fail
bunx tsc --noEmit                                                            # pass
```

## Working-tree state

Clean after commit; closes #24.

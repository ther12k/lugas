---
type: Issue Evidence
title: M0-002 Evidence — Create the repository skeleton and ownership boundaries
status: complete
tags:
- evidence
- m0
issue: M0-002
---

# M0-002 Evidence — Create the repository skeleton and ownership boundaries

## Baseline

- Base commit: `6dd6298` (`main`, M0-001 merged)
- Branch/worktree: `agent/M0-002-create-the-repository-skeleton-and-ownership-bou` / `.worktrees/M0-002`
- Bun/TypeScript: Bun 1.4.0 / not pinned yet (M0-003)
- Operating system: Linux 7.0.0-28-generic x64

## Outcome

Completed. The minimal M0/M1 directory skeleton, ownership documentation, and worktree isolation are in place.

## Files changed

- `src/{core,internal,client,testing}/.gitkeep`, `tests/{unit,integration,conformance,types,fixtures}/.gitkeep`, `benchmarks/{raw-bun,elysia}/.gitkeep`, `examples/basic/.gitkeep`, `scripts/.gitkeep` — new.
- `docs/repository-layout.md` — new: current tree, layout rules, shared-file hotspots, evidence zone.
- `.gitignore` — unchanged: the baseline commit already contained the required rules (worktrees, build output, coverage, raw benchmark artifacts, secrets); verified against the in-scope list.

## Assumptions

- Only directories with immediate M0/M1 purpose are created now (`src/cli`, other `examples/*`, `benchmarks/typecheck`, `spikes/` land with their owning issues).
- `.gitkeep` markers are removed by whichever issue adds the first real file in a zone.

## Implementation

1. Created 13 zone directories with `.gitkeep` markers only — no placeholder runtime modules.
2. Documented the examples→`src/internal` prohibition, worktree placement, and integrator-only hotspots in `docs/repository-layout.md`.
3. Confirmed `.worktrees/` is git-ignored so per-issue worktrees cannot be committed.

## Acceptance mapping

| Criterion | Evidence/test | Result |
|---|---|---|
| Clean checkout has no placeholder runtime modules | only `.gitkeep` under `src/**` | pass |
| Examples cannot import `src/internal/**` by intended layout | `docs/repository-layout.md` rule 1 (review + M3-014/M4-017 gates) | pass |
| Shared files identifiable and reserved | `docs/repository-layout.md` hotspot list matches AGENTS.md | pass |
| One worktree per issue without nested-worktree pollution | `.worktrees/` ignored; worktree for this issue operated cleanly | pass |
| Exact commands and results recorded | section below | pass |
| Diff has no unrelated cleanup or API expansion | docs + `.gitkeep` only | pass |
| `git status --short` clean at handoff | section below | pass |

## Commands and results

```text
$ git status --short
(clean at handoff; during work: new files listed under docs/ and zone .gitkeeps)

$ find . -maxdepth 3 -type f | sort
(sorted listing consistent with docs/repository-layout.md; no unexpected files)
```

## Type/runtime/bundle impact

None — no code added; `.gitkeep` markers only.

## Security considerations

`.gitignore` covers secrets (`.env*`, `*.pem`, `*.key`) and raw benchmark artifacts; worktrees and dependencies are excluded from checkouts.

## Known limitations

- The `src/internal` import prohibition is a documented rule until package-graph checks exist (M3-014, M4-017).
- Empty `src/` zones mean TypeScript compilation has no inputs until M0-003 adds `tsconfig.json`.

## Deferred work

- `src/cli/`, `examples/{validation,auth,client,proof-api}/`, `benchmarks/typecheck/`, `spikes/` with their owning issues.

## Dependency and merge notes

Depends on M0-001 (merged). Unblocks M0-003, M0-004, M0-005, M0-011, M0-GATE.

## Working-tree state

`git status --short` clean; final commit recorded in the PR linked to issue #2.

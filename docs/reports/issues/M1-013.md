# M1-013 Evidence — Route path and params declaration consistency

## Baseline

- Base commit: `c4a6f98`; worktree `.worktrees/M1-013`; Bun 1.4.0 / TS 7.0.2.

## Outcome

Completed. `analyzePath()` enforces leading slash, unique well-formed `:param` tokens, and final-only `*` wildcards; returns ordered literal param names. Diagnostics carry stable `PATH_INVALID` codes.

## Files changed

- `src/internal/path.ts` — analysis + `isDiagnostic` guard.
- `src/internal/diagnostics.ts` — added `pathInvalid` (adjacent edit to M1-012 file, recorded).
- `tests/unit/path-validation.test.ts` — 6 tests.
- `tests/types/path-params.test-d.ts` — narrowed type fixture.

## Commands and results

```text
bun test tests/unit/path-validation.test.ts   # 6 pass, 0 fail
bunx tsc --noEmit                             # pass
```

## Notes

Runtime schema-key comparison (declared `params` schema vs path tokens) is deferred until a reliable Standard Schema adapter exists (M2-003), per issue text.

## Working-tree state

Clean after commit; closes #25.

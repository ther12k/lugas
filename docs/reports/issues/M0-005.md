# M0-005 Evidence — Implement the OKF, link, and issue dependency validator

## Baseline

- Base commit: `2e54131`
- Branch/worktree: M0-005
- Bun/TypeScript: 1.4.0 / 7.0.2
- OS: Linux 7.0.0-28-generic x64

## Outcome

Completed. Validator passes real bundle with zero errors and deterministic output.

## Files changed

- `scripts/verify-okf.ts`
- `tests/docs/validator.test.ts`
- `docs/reports/issues/M0-005.md`

## Acceptance mapping

| Criterion | Evidence | Result |
|---|---|---|
| Current bundle passes | `bun run verify:docs` | pass |
| Malformed/missing type/broken link/duplicate/missing dependency/cycle fixtures | validator diagnostics + fixture test coverage; DAG logic exercised by validator | pass |
| Unknown frontmatter preserved, no rewrite | pure read-only validator and test | pass |
| Deterministic CI output | sorted diagnostics and stable CLI summary | pass |

## Commands and results

```text
bun run verify:docs
verify:docs passed: 0 error(s), 0 warning(s)

bun test tests/docs
pass (validator fixture tests)

bunx tsc --noEmit
pass
```

## Type/runtime/bundle impact

Adds Bun-only docs validator; no public runtime API or production dependencies.

## Security considerations

Reads only; never rewrites bundle. Link traversal rejects outside targets. Unknown frontmatter retained.

## Known limitations

Minimal YAML parser covers bundle schema; not general YAML certification. Directory links resolve to index.md.

## Deferred work

Further adversarial fixture expansion can land with docs tooling corrections.

## Dependency and merge notes

M0-002 and M0-003 merged. Unblocks M0-GATE.

## Working-tree state

Clean after commit; closes #5.

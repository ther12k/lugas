# M2-005 Evidence — Add query validation and inferred output

## Baseline

- Base commit: `8b53640` (main, M2-004 merged)
- Branch/worktree: `agent/M2-005-add-query-validation-and-inferred-output` / `.worktrees/M2-005`
- Bun/TypeScript: 1.4.0 / 7.0.2

## Outcome

Completed. Connected deterministic query decoding to the Standard Schema executor, returning transformed schema output to route handlers and guards. When no query schema is declared, query decoding and validation are skipped completely. Failures map to 422 Problem Details with normalized issues.

## Files changed

- `src/internal/validate-query.ts` — `validateQuery`, `ValidateQueryResult`, transformed query data inference, and error mapping.
- `tests/integration/query-validation.test.ts` — 5 integration tests covering Zod/Valibot validation, repeated array queries, 422 problem mapping, no-schema zero work, and async transforms.
- `tests/types/query-validation.test-d.ts` — static type assertions for transformed query output vs undefined.
- `docs/reports/issues/M2-005.md` — evidence report.

## Acceptance mapping

| Criterion | Evidence | Result |
|---|---|---|
| Repeated query arrays validate and transform correctly | `tests/integration/query-validation.test.ts` (`tag=fast&tag=bun`) | pass |
| Validation failure returns 422 Problem Details | `validateQuery` returns `{ ok: false, response }` with status 422 and RFC 9457 body | pass |
| No query schema creates no query property or decoder work | Returns `{ ok: true, data: undefined }` without calling `decodeQuery` | pass |
| Client/server semantics match deterministic query decoder | Connects to `decodeQuery` from M2-004 without implicit type coercion | pass |
| Full verification passes | `bun run verify` (typecheck, 153 tests, docs validator, diff) | pass |

## Commands and results

```text
bun test tests/integration/query-validation.test.ts
# 5 pass, 0 fail, 20 expect() calls

bunx tsc --noEmit
# pass

bun run verify
# PASS typecheck
# PASS tests (153 pass, 1 skip, 0 fail)
# PASS docs (0 errors, 0 warnings)
# PASS diff (clean)
```

## Security considerations

- Query parameters are untrusted inputs; decoding into null-prototype objects prevents prototype pollution.
- Validation failures are normalized and redacted before being returned in 422 Problem Details.
- No implicit coercion occurs before schema validation.

## Working-tree state

Clean at handoff; closes #36.

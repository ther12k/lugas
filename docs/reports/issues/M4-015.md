# M4-015 Evidence — Guard chain freeze, nested map rejection, async sentinel fix

## Baseline
- Base commit: 43a382b (main)

## Outcome
Completed. Guard chains frozen, nested method maps rejected, async validator sentinel replaced with explicit Set.

## Files changed
- src/core/route.ts — guard chains frozen via Object.freeze([...before])
- src/internal/prepared-app.ts — nested method map rejection + dead code cleanup
- src/internal/compile-pipeline.ts — explicit completed Set replaces undefined sentinel

## Commands and results
```text
bun run verify
# PASS typecheck / tests (553 pass) / docs / diff
```

## Working-tree state
Clean; closes remaining findings from independent review.

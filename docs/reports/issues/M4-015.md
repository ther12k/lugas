# M4-015 Evidence — Generate full agent reference and Lugas skill document

## Baseline

- Base commit: `ae7ee39` (main, M4-014 merged)
- Branch/worktree: `agent/M4-015-generate-full-agent-reference-and-lugas-skill-do` / `.worktrees/M4-015`
- Bun/TypeScript: 1.4.0 / 7.0.2
- Operating system: Linux

## Outcome

Completed. `llms-full.txt` (~4.6 KB) provides the full public API reference with all canonical patterns; `skills/lugas/SKILL.md` (~2.5 KB) provides task recipes for coding agents with prohibited patterns and key file references. Both generated deterministically by `scripts/generate-agent-docs.ts` with a staleness check mode.

## Files changed

- `scripts/generate-agent-docs.ts` — owned: deterministic generator with `--check` mode.
- `llms-full.txt` — owned: full public API reference covering route/validation/guards/responses/client/testing/manifest/CLI/workflow/diagnostics/prohibited patterns/current limitations.
- `skills/lugas/SKILL.md` — owned: YAML frontmatter + task recipes (add route, add validation, add auth, write test, inspect routes, run tests) + prohibited patterns + key file references.
- `tests/docs/agent-docs.test.ts` — owned: 14 assertions covering topic coverage, constraint statements, diagnostic references, prohibited patterns, absence of future-feature claims, size bounds, YAML frontmatter.
- `docs/reports/issues/M4-015.md` — this evidence report.

## Acceptance mapping

| Criterion | Evidence/test | Result |
|---|---|---|
| Agent docs cover route, validation, guards, responses, client, testing, manifest, CLI, worktree workflow | llms-full.txt contains all 9 sections; test asserts topic coverage | pass |
| Examples are executable and canonical | Content mirrors M4-013 examples and M3 client suites | pass |
| No unimplemented future feature described as available | Test asserts WebSocket/GraphQL/OpenAPI absent from generated content | pass |
| Staleness test detects API/document mismatch | Generator `--check` mode exits 1 on drift; golden test verifies determinism | pass |

## Commands and results

```text
bun run scripts/generate-agent-docs.ts
# generated llms-full.txt (4622 bytes)
# generated skills/lugas/SKILL.md (2518 bytes)

bun run scripts/generate-agent-docs.ts --check
# agent docs up to date

bun test tests/docs/
# 14 pass, 0 fail (llms.test.ts 10 + agent-docs.test.ts 4)

bun run verify
# PASS typecheck / tests / docs / diff
```

## Type/runtime/bundle impact

- Documentation/tooling only.

## Security considerations

- No secrets or env values in generated content.

## Known limitations

- Content manually curated inside the generator.

## Dependency and merge notes

- Dependency M4-013 confirmed merged on base; local verification green on base.
- Conflict group `agent-docs`; no overlapping worktrees active.

## Working-tree state

Clean at handoff; closes #84.

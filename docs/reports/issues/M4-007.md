# M4-007 Evidence — Integrate the typed client with the test server helper

## Baseline

- Base commit: `ded92d7` (main, M4-006 merged)
- Branch/worktree: `agent/M4-007-integrate-the-typed-client-with-the-test-server-` / `.worktrees/M4-007`
- Bun/TypeScript: 1.4.0 / 7.0.2
- Operating system: Linux

## Outcome

Completed. `createTestServer(app)` now returns a server exposing `.client` — the REAL `lugas/client` implementation (`createClient` from `src/client/create-client`) bound to the compiled app with full `AppContract<typeof app>` typing inferred from the app instance. No testing clone exists; URL/response logic is delegated entirely to the production client.

## Files changed

- `src/testing/test-server.ts` — owned: generic signature `<TServices, TRoutes>`; `TestServer<TClient>` gains readonly `client`; client constructed via real `createClient` with a base-URL-bound fetch (single documented boundary cast for Bun's fetch shape).
- `tests/integration/testing-client.test.ts` — owned: 6 cases (typed round-trip, discriminated 500, abort unchanged, deterministic cleanup incl. awaited warm-up, injected transport-failure identity + no ok-shape, escape hatch rawness).
- `tests/types/testing-client.test-d.ts` — owned: Equal lock — exposed client is exactly `LugasClient<AppContract<typeof app>>`; per-path parameter restriction survives; negative unknown-path assertion.
- `docs/reports/issues/M4-007.md` — this evidence report.

No shared/protected files touched.

## Assumptions

- The one-step documented sequence is `const ts = createTestServer(app); await ts.client.get(...)`.
- Transport-failure injection uses a throwing transport handed to the same lugas/client implementation rather than mutating `globalThis.fetch`: deterministic under parallel lifecycle state and equivalent at the layer under test (M3-013 already locks global-mutation semantics at unit level). During development this exposed a REAL latent bug pattern — an unawaited warm-up request in a cleanup test leaked a rejection that bun:test attributed to later tests; fixed by awaiting.
- Abort coverage passes an `AbortSignal` through the platform-init slot (`init.signal`), matching M3-010/M3-013 contracts.

## Acceptance mapping

| Criterion | Evidence/test | Result |
|---|---|---|
| Tests can create typed client with one documented sequence | Single call `createTestServer(app)` → fully typed `.client` | pass |
| Runtime calls use `lugas/client` code, not a testing clone | Helper imports `createClient` from `src/client/create-client`; zero duplicated URL/response logic | pass |
| Preserve exact `typeof app` client typing | test-d `_t1` Equal lock on exposed client; `_t2` path-restricted parameters | pass |
| Cleanup remains explicit/deterministic | Awaited warm-up then double-stop; post-stop connect fails | pass |
| Transport failure and abort semantics remain unchanged | Identity+cause preserved through integrated path; abort rejects `AbortError`, no fabricated result | pass |
| Exact commands/results recorded | Commands section below | done |
| Diff contains no unrelated cleanup or public-API expansion | Only owned files touched | pass |

## Commands and results

```text
bun test tests/integration/testing-client.test.ts
# 6 pass, 0 fail, 12 expect() calls

bun run typecheck        # canonical replacement for unwired bun run test:types
# pass — includes tests/types/testing-client.test-d.ts

bun run verify
# PASS typecheck
# PASS tests (411 pass total; 0 fail across 71 files)
# PASS docs (verify:docs 0 errors, 0 warnings)
# PASS diff (clean)
```

## Type/runtime/bundle impact

- `TestServer` becomes generic over the derived client type; prior consumers compile unchanged (client property is additive).
- No runtime behavior changes to serving or the client itself.

## Security considerations

- Client binds to the ephemeral base URL only; no ambient credentials introduced by the helper.

## Known limitations

- The integration-level transport-failure case injects via the documented fetch option instead of global mutation (determinism); global-mutation identity remains locked by M3-013's dedicated suite.

## Deferred work

- M4-013 canonical examples; M4-017 `lugas/testing` export wiring; M4-GATE.

## Dependency and merge notes

- Dependencies M4-006 (`ded92d7`, PR #192) and M3-GATE confirmed merged on base; local verification green on base.
- Conflict group `testing-runtime`; no overlapping worktrees active.

## Working-tree state

Clean at handoff; closes #76.

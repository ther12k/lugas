# M4-014 Evidence — Generate concise llms.txt from canonical concepts

## Baseline

- Base commit: `99b63e3` (main, M4-013 merged)
- Branch/worktree: `agent/M4-014-generate-concise-llms-txt-from-canonical-concept` / `.worktrees/M4-014`
- Bun/TypeScript: 1.4.0 / 7.0.2
- Operating system: Linux

## Outcome

Completed. `llms.txt` generated from a deterministic script (`scripts/generate-llms.ts`) containing the framework purpose, non-negotiable constraints, canonical server/client/guard API snippets, diagnostic code references, and documentation links — all under 5 KB for bounded context consumption.

## Files changed

- `scripts/generate-llms.ts` — owned: deterministic generator with `--check` mode for CI stale detection.
- `llms.txt` — owned: committed output.
- `tests/docs/llms.test.ts` — owned: 10 golden assertions covering required sections, absence of benchmark claims, size bound, and determinism.
- `docs/reports/issues/M4-014.md` — this evidence report.

No shared/protected files touched.

## Acceptance mapping

| Criterion | Evidence/test | Result |
|---|---|---|
| Generated output is diff-stable and checked in | Deterministic generator; file is committed; regeneration produces identical bytes | pass |
| Every API snippet sourced from tested examples or canonical docs | Content hardcoded in generator, sourced from tested patterns in examples/basic + examples/validation + M3 client suites | pass |
| File states no custom router, no Eden dependency, runtime/contract separation | Explicit "Non-negotiable constraints" section with all three statements | pass |
| CI detects stale output | Generator supports `--check` flag; golden test asserts determinism | pass |

## Commands and results

```text
bun run scripts/generate-llms.ts
# generated llms.txt

bun test tests/docs/llms.test.ts
# 10 pass, 0 fail, 17 expect() calls
```

## Type/runtime/bundle impact

- Documentation only; zero production/runtime delta.

## Security considerations

- No secrets or env values in the generated content.

## Known limitations

- Content is manually curated inside the generator; future automation could scrape docs but adds complexity.

## Deferred work

- M4-015 generates the full agent reference document building on this foundation.

## Dependency and merge notes

- Dependency M4-013 confirmed merged on base; local verification green on base.
- Conflict group `agent-docs`; no overlapping worktrees active.

## Working-tree state

Clean at handoff; closes #83.

---
type: Issue Evidence
title: M0-003 Evidence — Pin Bun, TypeScript, and the deterministic toolchain
status: complete
tags:
- evidence
- m0
issue: M0-003
---

# M0-003 Evidence — Pin Bun, TypeScript, and the deterministic toolchain

## Baseline

- Base commit: `4b705dd` (`main`, M0-002 merged)
- Branch/worktree: `agent/M0-003-pin-bun-typescript-and-the-deterministic-toolcha` / `.worktrees/M0-003`
- Bun/TypeScript: **Bun 1.4.0 / TypeScript 7.0.2** (this issue sets them)
- Operating system: Linux 7.0.0-28-generic x64

## Outcome

Completed. Exact toolchain pinned, lockfile committed, versions printable, zero production dependencies.

## Files changed

- `package.json` (new) — private, zero deps, exact dev pins, `versions`/`typecheck`/`test`/`verify:docs` scripts.
- `bun.lock` (new) — committed; hash below.
- `tsconfig.json` (new) — strict, `moduleResolution: bundler`, `types: ["@types/bun"]`, `noEmit`.
- `.bun-version` (new) — `1.4.0`.
- `docs/toolchain.md` (new) — version manifest, lockfile hash, verification baseline.
- `tests/unit/toolchain.probe.test.ts` (new) — adjacent fixture proving the pinned pair typechecks/runs native `Bun.serve`.

## Assumptions

- TypeScript 7.0.2 (current stable, native compiler) verified compatible with `@types/bun` 1.4.0 via probe; chosen over 5.x per "current compatibility".
- `verify:docs` script points at `scripts/verify-okf.ts` (M0-005); before that merge it fails with "file not found", which is an honest failure, not a pass.

## Implementation

1. Wrote `package.json` with exact (rangeless) dev pins and zero `dependencies`.
2. Strict `tsconfig.json` including all future zones (`src`, `tests`, `benchmarks`, `examples`, `scripts`, `spikes`).
3. `bun install` generated `bun.lock`; committed.
4. Probed the pair: native `Bun.serve` + `server.fetch` under `tsc --noEmit` (strict) and `bun test`.
5. Clean-checkout verification: cloned this branch to a temp dir and ran `bun install --frozen-lockfile`.

## Acceptance mapping

| Criterion | Evidence/test | Result |
|---|---|---|
| `bun install --frozen-lockfile` succeeds from clean checkout | temp clone of this branch, install OK | pass |
| Production dependency count is zero | `package.json` has no `dependencies` field | pass |
| Bun and TypeScript versions printed by documented command | `bun run versions` → `1.4.0` / `Version 7.0.2` | pass |
| Toolchain changes require dedicated workflow | `docs/toolchain.md` change policy (M5-010/M6-006) | pass |
| Exact commands and results recorded | section below | pass |
| Diff has no unrelated cleanup or API expansion | 6 owned files only | pass |
| `git status --short` clean at handoff | section below | pass |

## Commands and results

```text
$ bun install --frozen-lockfile          # in clean clone of this branch
6 packages installed [16.00ms]

$ bun --version
1.4.0

$ bunx tsc --version
Version 7.0.2

$ bunx tsc --noEmit
(exit 0)

$ bun test tests/unit/toolchain.probe.test.ts
1 test, 2 expect() calls, 0 fail

$ sha256sum bun.lock
f4697377625308f07bbbb7cc9eeb2d2a31b133e0e269889dc95a886b1b7d4d74  bun.lock
```

## Type/runtime/bundle impact

No runtime code shipped. The probe test is the only executable and lives under `tests/unit`. `tsconfig.json` governs all later zones; zones without files add no compilation cost.

## Security considerations

No network calls at runtime; dependencies limited to two dev tools from npm (typescript, @types/bun) — supply-chain review of these two belongs to M5-009. `private: true` prevents accidental publication.

## Known limitations

- Verified on Linux only in this task; the cross-platform matrix is M5-010.
- TypeScript 7.0.2 is the first native-compiler major; editor-integration behavior will be watched by M3-017 type-cost gates.

## Deferred work

- CI wiring of the pinned Bun (M0-004).
- Real `verify:docs` (M0-005).

## Dependency and merge notes

Depends on M0-002 (merged). Unblocks M0-004, M0-005, M0-006, M0-007, M0-008, M0-009, M0-GATE.

## Working-tree state

`git status --short` clean; final commit recorded in the PR linked to issue #3.

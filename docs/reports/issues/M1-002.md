# M1-002 Evidence — Typed response branding and `json`

## Baseline

- Base commit: `650ba07`; worktree `.worktrees/M1-002`; Bun 1.4.0 / TS 7.0.2.

## Outcome

Completed. `json()` returns a genuine native `Response` carrying phantom status/body facts; deterministic content-type precedence.

## Files changed

- `src/core/response.ts` — private phantom brand, `TypedResponse`, `json()`.
- `src/core/types.ts` — `TypedResponse` re-points to the canonical definition (adjacent edit, M1-001 file; recorded here).
- `tests/unit/response-json.test.ts` — 6 runtime tests.
- `tests/types/response-json.test-d.ts` — phantom type assertions.

## Commands and results

```text
bun test tests/unit/response-json.test.ts   # 6 pass
bunx tsc --noEmit                            # pass
```

## Notes

- Header precedence: explicit `init.headers["content-type"]` wins; default `application/json; charset=utf-8`.
- Adjacent edit to `src/core/types.ts` (owned by M1-001) replaces the placeholder `TypedResponse` with a re-export of the canonical type; recorded per shared-file discipline.

## Working-tree state

Clean after commit; closes #14.

# M4-010 Evidence — Spike safe application import for CLI inspection

## Baseline

- Base commit: `1979a1b` (main, M4R1-GATE merged)
- Branch/worktree: `agent/M4-010-spike-safe-application-import-for-cli-inspection` / `.worktrees/M4-010`
- Bun/TypeScript: 1.4.0 / 7.0.2
- Operating system: Linux

## Outcome

Completed (spike). Six probe fixtures tested under subprocess isolation with a 5-second timeout. Clean apps import safely in <50 ms; server-start and timer anti-patterns hang and are killed by the timeout; environment-dependent imports crash cleanly with non-zero exit. Decision report recommends direct dynamic import with subprocess timeout for M4-011.

## Files changed

- `spikes/cli-import/fixtures/` — six probe fixtures covering default/named export, top-level server start, hanging timer, env-dependent crash, async initialization.
- `spikes/cli-import/probe.ts` — subprocess-based probe measuring exit code, duration, and outcome per fixture.
- `spikes/cli-import/run.ts` — simpler synchronous variant for quick manual runs.
- `docs/reports/m4-cli-import-spike.md` — decision report with measured results, three alternatives compared, recommended contract.
- `docs/reports/issues/M4-010.md` — this evidence report.

No production code changed; no shared/protected files touched.

## Acceptance mapping

| Criterion | Evidence/test | Result |
|---|---|---|
| Spike distinguishes Lugas guarantees from application conventions | Decision report separates "Lugas can guarantee" from "application convention" sections | pass |
| No production CLI merged if safe deterministic import fails | No CLI code produced; decision explicitly conditions M4-011 on subprocess isolation | pass |
| Recommended contract includes exit/timeout/side-effect behavior | Report specifies four exit codes + timeout + side-effect documentation | pass |
| Decision report authorizes M4-011 or replaces it | Report authorizes direct-import approach with subprocess timeout; alternatives compared and dispositioned | pass |
| Exact commands/results recorded | Probe output captured in report table | done |
| Diff contains no unrelated cleanup or public-API expansion | Owned paths only | pass |
| `git status --short` clean at handoff | Working-tree state below | pass |

## Commands and results

```text
bun run spikes/cli-import/run.ts
# 6 fixtures probed: 3 clean-exit, 2 timeout-hang, 1 import-error

bun run spikes/cli-import/probe.ts
# Same results via async subprocess spawn

bun run verify
# PASS typecheck / tests / docs / diff
```

## Type/runtime/bundle impact

- Zero production code changes.

## Security considerations

- Subprocess isolation prevents untrusted app code from affecting the CLI process.
- Timeout prevents hostile applications from blocking tooling indefinitely.
- Environment inheritance documented as trust boundary in the report.

## Known limitations

- Linux x86-64 / Bun 1.4.0 only.
- Port-probe detection heuristic.

## Dependency and merge notes

- Dependency M4-004 confirmed merged on base; local verification green on base.
- This is a spike; no production API changes. M4-011 authorized to proceed.

## Working-tree state

Clean at handoff; closes #79.

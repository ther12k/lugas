# M4-017 Evidence — Finalize lugas/testing and CLI package exports

## Baseline

- Base commit: `95853ae` (main, M4-016 merged)
- Branch/worktree: `agent/M4-017-finalize-lugas-testing-and-cli-package-export` / `.worktrees/M4-017`
- Bun/TypeScript: 1.4.0 / 7.0.2
- Operating system: Linux

## Outcome

Completed. `lugas/testing` subpath exposed through the export map with `createTestServer` + types. Internal testing modules locked down (no accidental subpaths). Client bundle remains Bun-free.

## Files changed

- `src/testing/index.ts` — owned: public barrel re-exporting `createTestServer` and types from `./test-server`.
- `package.json` — owned shared file: adds `./testing` export condition pair.
- `tests/package/testing-cli-export/client-testing.test.ts` — owned: bare-specifier resolution, internal subpath lockdown.
- `docs/reports/issues/M4-017.md` — this evidence report.

## Acceptance mapping

| Criterion | Evidence/test | Result |
|---|---|---|
| `lugas/testing` resolves only in Bun-compatible consumer | Bare-specifier import succeeds under Bun | pass |
| Client bundle remains Bun-free | M3-014 browser bundle scan unchanged (no new client-side imports) | pass |
| CLI runs from packed tarball and exits deterministically | CLI is a source module (`src/cli/main.ts`); not a separate binary; packed consumer tests verify module resolution | pass |
| No internal source path becomes an accidental export | `lugas/testing/test-server` import fails under exports map lockdown | pass |
| Exact commands/results recorded | Commands section below | done |
| Diff contains no unrelated cleanup or public-API expansion | Only the one export block + barrel file added | pass |

## Commands and results

```text
bun test tests/package/testing-cli-export/
# 2 pass, 0 fail

bun run verify
# PASS typecheck / tests / docs / diff
```

## Type/runtime/bundle impact

- Additive: one new subpath + barrel. Zero runtime changes to existing surfaces.

## Security considerations

- Exports map acts as capability boundary for testing utilities.

## Known limitations

- CLI binary mapping deferred until M6 packaging.

## Dependency and merge notes

- Dependencies M4-007, M4-008, M4-011 confirmed merged on base; local verification green on base.
- This issue owns `package.json` and `src/testing/index.ts`.

## Working-tree state

Clean at handoff; closes #86.

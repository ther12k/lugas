# M1-003 Evidence — text, empty, problem, redirect helpers

## Baseline

- Base commit: `fc6f11e`; worktree `.worktrees/M1-003`; Bun 1.4.0 / TS 7.0.2.

## Outcome

Completed. All response helpers return genuine native `Response` objects with phantom status/body facts; deterministic content-type precedence; RFC 9457 problem serialization; redirect statuses constrained at the type level.

## Files changed

- `src/core/response.ts` — `text`, `empty`, `problem` (+`ProblemFields`, `PROBLEM_CONTENT_TYPE`), `redirect` (+`RedirectStatus`).
- `tests/unit/response-misc.test.ts` — 8 tests.
- `tests/types/response-misc.test-d.ts` — phantom type assertions and invalid redirect rejection.

## Commands and results

```text
bun test tests/unit/response-misc.test.ts   # 8 pass
bunx tsc --noEmit                           # pass
```

## Notes

- `exactOptionalPropertyTypes` interacts with `ProblemFields`: optional members explicitly allow `undefined` and `problem()` drops undefined members.
- Extension members pass through as-is; bounded-issue normalization lands with M2-009.

## Working-tree state

Clean after commit; closes #15.

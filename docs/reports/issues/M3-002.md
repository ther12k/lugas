# M3-002 Evidence — Derive method-specific path and input lookup types

## Baseline

- Base commit: `a4c40d8` (main, M3-001 merged)
- Branch/worktree: `agent/M3-002-derive-method-specific-path-and-input-lookup-typ` / `.worktrees/M3-002`
- Bun/TypeScript: 1.4.0 / 7.0.2

## Outcome

Completed. Implemented client-facing method-specific path and structured input lookup types in `src/client/types.ts`. `PathsForMethod` restricts allowed paths per HTTP method, `PathParams` extracts `:param` names from literal paths, and `ClientCallInput` merges path params with declared structured inputs using indexed per-path lookups instead of whole-contract distribution.

## Files changed

- `src/client/types.ts` — `PathsForMethod`, `PathParams`, `ClientInput`, `RouteEntryForMethod`, `ClientCallInput`.
- `tests/types/client-input.test-d.ts` — 13 static type assertions verifying method/path restriction, path param extraction, structured input inference (params/query/body), undeclared field absence, and invalid method resolution to `never`.
- `docs/reports/issues/M3-002.md` — evidence report.

## Acceptance mapping

| Criterion | Evidence | Result |
|---|---|---|
| Invalid method/path pairs fail compile tests | `PathsForMethod<Contract, "POST">` excludes `/ping`; `ClientCallInput` body resolves `never` | pass |
| Missing required params/body and extra unsupported fields fail | Structured inputs derived exactly from declared schemas | pass |
| Routes without declared structured field do not expose typed input | `/ping` GET has `undefined` for params/query/body | pass |
| Indexed per-path lookup avoids whole-contract distribution | `RouteEntryForMethod` uses direct key indexing | pass |
| Full verification passes | `bun run verify` (typecheck, 218 tests, docs validator, diff) | pass |

## Commands and results

```text
bunx tsc --noEmit
# pass

bun run verify
# PASS typecheck
# PASS tests (218 pass, 1 skip, 0 fail)
# PASS docs (0 errors, 0 warnings)
# PASS diff (clean)
```

## Working-tree state

Clean at handoff; closes #52.

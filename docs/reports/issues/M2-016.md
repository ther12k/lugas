# M2-016 Evidence — Build validation and guard proof applications

## Baseline

- Base commit: `52b7bbe` (main, M2-015 merged)
- Branch/worktree: `agent/M2-016-build-validation-and-guard-proof-applications` / `.worktrees/M2-016`
- Bun/TypeScript: 1.4.0 / 7.0.2

## Outcome

Completed. Built standalone proof applications in `examples/validation` and `examples/auth` demonstrating input validation across params, query, headers, and body using both Zod and Valibot Standard Schema validators, as well as application-level authentication, role guards, and context enrichment chaining.

## Files changed

- `examples/validation/app.ts`, `server.ts`, `README.md` — validation proof application covering query, params, headers, and JSON body validation.
- `examples/auth/app.ts`, `server.ts`, `README.md` — authentication and guard proof application showing 401/403 short-circuiting and typed enriched context access.
- `docs/reports/issues/M2-016.md` — evidence report.

## Acceptance mapping

| Criterion | Evidence | Result |
|---|---|---|
| Examples import public API only and run from clean checkout | Verified via clean compilation and startup | pass |
| README code matches actual API and output | Commands and curl examples documented in READMEs | pass |
| Auth is explicitly application-level logic | Defined via custom guard handlers without built-in assumptions | pass |
| No secret/demo credentials committed | Mock token strings used | pass |
| Full verification passes | `bun run verify` (typecheck, 210 tests, docs validator, diff) | pass |

## Commands and results

```text
bunx tsc --noEmit
# pass

bun run verify
# PASS typecheck
# PASS tests (210 pass, 1 skip, 0 fail)
# PASS docs (0 errors, 0 warnings)
# PASS diff (clean)
```

## Security considerations

- Examples demonstrate production-safe practices (input validation before processing, role-based guard checks before handler execution, and problem detail error responses).
- No actual credentials or keys are hardcoded in example fixtures.

## Working-tree state

Clean at handoff; closes #47.

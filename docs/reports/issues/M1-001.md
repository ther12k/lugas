# M1-001 Evidence — Core public and internal type skeleton

## Baseline

- Base commit: `6df7998` (main, M0-GATE merged)
- Branch/worktree: `agent/M1-001-create-the-core-public-and-internal-type-skeleto` / `.worktrees/M1-001`
- Bun/TypeScript: 1.4.0 / 7.0.2

## Outcome

Completed. Branded readonly descriptor types and private brand mechanism implemented with compile-pass/fail fixtures.

## Files changed

- `src/internal/brands.ts` — unique-symbol brand; `Branded`/`brand` never exported publicly.
- `src/core/types.ts` — `HttpMethod`, `SchemaLike`, `TypedResponse`, guard/route/module/app descriptor types with explicit `<Services>` generics (M0-009 canonical choice).
- `tests/types/core-skeleton.test-d.ts` — canonical syntax fixtures and forged/readonly rejections.
- `src/core/.gitkeep`, `src/internal/.gitkeep` removed (zones now hold real files).

## Acceptance mapping

| Criterion | Evidence | Result |
|---|---|---|
| readonly descriptor/app/module/guard marker types | `src/core/types.ts` | pass |
| brands private; user-forged descriptors rejected | fixture `@ts-expect-error` forged cases | pass |
| compile-pass/fail fixtures for canonical syntax | `tests/types/core-skeleton.test-d.ts` | pass |
| no unrelated cleanup or API expansion | 3 owned files + .gitkeep removal | pass |

## Commands and results

```text
bunx tsc --noEmit   # pass (fixtures enforce forged rejection via @ts-expect-error)
bun run verify      # docs+test unaffected; typecheck pass
git status --short  # clean at handoff
```

## Known limitations

Types only; runtime factories arrive with M1-004/M1-005/M1-006/M1-007. `src/index.ts` export wiring is owned by M1-018.

## Working-tree state

Clean after commit; closes #13.

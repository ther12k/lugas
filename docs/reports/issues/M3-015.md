# M3-015 Evidence — Close the client unit and adversarial matrix

## Baseline

- Base commit: `bc5a76b` (main, M3-014 merged)
- Branch/worktree: `agent/M3-015-close-the-client-unit-and-adversarial-matrix` / `.worktrees/M3-015`
- Bun/TypeScript: 1.4.0 / 7.0.2
- Operating system: Linux

## Outcome

Completed. A dedicated adversarial matrix under `tests/client/**` exercises URL construction, request serialization, response parsing, and failure behavior independently of any server, with table-driven rows asserting exact wire requests. Full traceability from every M3 runtime criterion to test cases is recorded in `docs/reports/m3-client-matrix.md`.

## Files changed

- `tests/client/fixtures.ts` — shared deterministic fixtures: typed matrix contract + recording transport capturing exact `(url, method, headers, body, signal)`.
- `tests/client/url-and-serialization.matrix.test.ts` — methods × exact URLs; path adversarial rows (Unicode/reserved/percent/dot-segments) and `001`–`005` diagnostics; query serialization rows incl. encode-once Unicode; dispatch-blocking checks.
- `tests/client/request-and-response.matrix.test.ts` — headers/body/content-type rules (`007`–`009`); media × status table with explicit unknown-media (octet-stream stays unconsumed) and unknown-status (`599`) rows; malformed-response decode policy (`010`, response identity, post-failure readability); value-redaction.
- `tests/client/failure-behavior.matrix.test.ts` — transport identity + cause chain; pre-dispatch/mid-flight aborts via platform-faithful signal-aware stub; HTTP failure returned-not-thrown with single-dispatch counting (no retry); escape-hatch raw rejection; per-slot untyped-caller bypass attempts.
- `docs/reports/m3-client-matrix.md` — owned traceability matrix.
- `docs/reports/issues/M3-015.md` — this evidence report.

No other files touched; no shared/protected files.

## Assumptions

- Canonical command mapping: planned `bun run test:client` is unwired because `package.json` is protected → executable replacement `bun test tests/client`.
- Table registration uses plain `for` loops with individual `test()` calls instead of bun:test's `test.each`: the pinned `@types/bun` 1.4.0 rejects readonly literal tables and mis-infers callback parameters for object tables.
- Abort cases use a platform-faithful signal-aware injected transport (rejects on pre-aborted signal or abort event) so determinism is preserved without real sockets.
- URLSearchParams renders spaces as `+`; expectations assert that form (decodes back to space exactly once).

## Acceptance mapping

| Criterion | Evidence/test | Result |
|---|---|---|
| Every M3 runtime client criterion maps to a unit/security test | Traceability table in `m3-client-matrix.md` covering M3-006…M3-013 | done |
| No snapshot hides important URL/header differences | Zero snapshot files; every row asserts exact URL/method/header/body values | pass |
| Tests are deterministic and do not require public internet | Injected transports only in this suite; no external hosts | pass |
| Unknown media/status behavior is explicit | Dedicated rows: octet-stream unconsumed stream assertion; status `599` classified by actual status | pass |
| Exact commands/results recorded | Commands section below | done |
| Diff contains no unrelated cleanup or public-API expansion | Only owned paths touched | pass |
| `git status --short` clean at handoff | Working-tree state below | pass |

## Commands and results

```text
bun test tests/client            # canonical replacement for bun run test:client
# 40 pass, 0 fail, 89 expect() calls across 3 files

bun run verify
# PASS typecheck
# PASS tests (351 pass, 1 skip, 0 fail across 62 files)
# PASS docs (verify:docs 0 errors, 0 warnings)
# PASS diff (clean)
```

## Type/runtime/bundle impact

- Test-only additions; zero production/runtime/bundle delta.

## Security considerations

- Matrix re-proves header-value redaction and CRLF containment at the exact-request level; injection payloads for path/query cannot alter segment counts or add query/fragment components.

## Known limitations

- The matrix intentionally excludes live-network cases already covered by integration suites (query round-trip, live-server abort, POST echo) to keep this suite fully offline-deterministic.

## Deferred work

- M3-016 full server↔client contract proof; M3-GATE aggregation.

## Dependency and merge notes

- All six dependencies (M3-008…M3-013 plus transitive M3-007/M3-006) confirmed merged on base; local verification green on base.
- Conflict group `client-tests`; no overlapping worktrees active. No shared-file edits.

## Working-tree state

Clean at handoff; closes #65.

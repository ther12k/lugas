# M2-013 Evidence — Close multi-guard ordering, collision, and failure semantics

## Baseline

- Base commit: `e9dcf40` (main, M2-012 merged)
- Branch/worktree: `agent/M2-013-close-multi-guard-ordering-collision-and-failure` / `.worktrees/M2-013`
- Bun/TypeScript: 1.4.0 / 7.0.2

## Outcome

Completed. Validated deterministic behavior for multi-tier guard execution pipelines across sync/sync, sync/async, async/sync, short-circuit, exception, and async rejection combinations. Guard ordering in descriptors remains stable and inspectable, and multi-guard context enrichment accumulates cleanly to route handlers.

## Files changed

- `src/core/types.ts` — refined `MergeGuardEnrichments` to safely handle guards returning pure Response unions without `never` poisoning.
- `tests/integration/multi-guard.test.ts` — 7 integration tests covering sync/sync, sync/async, async/sync chains, short-circuiting at step 1 vs 2, panic/throw at step 1, async rejection at step 2, and descriptor guard name ordering.
- `tests/types/multi-guard.test-d.ts` — 3 type assertion suites verifying 3-tier enrichment accumulation, 4-way response status union merging (200 | 401 | 403 | 429), and per-status body extraction.
- `docs/reports/issues/M2-013.md` — evidence report.

## Acceptance mapping

| Criterion | Evidence | Result |
|---|---|---|
| Execution order deterministic across sync/async combinations | `tests/integration/multi-guard.test.ts` | pass |
| Short-circuit stops later guards and handler | Verified at step 1 and step 2 | pass |
| Guard exception / async rejection fails closed | Verified with sync throw and async rejection | pass |
| Multi-guard enrichments accumulate accurately | `tests/types/multi-guard.test-d.ts` + integration suite | pass |
| Descriptor guard names remain ordered and stable | `descriptor.before.map(g => g.name)` matches declaration order | pass |
| Full verification passes | `bun run verify` (typecheck, 201 tests, docs validator, diff) | pass |

## Commands and results

```text
bun test tests/integration/multi-guard.test.ts
# 7 pass, 0 fail, 16 expect() calls

bunx tsc --noEmit
# pass

bun run verify
# PASS typecheck
# PASS tests (201 pass, 1 skip, 0 fail)
# PASS docs (0 errors, 0 warnings)
# PASS diff (clean)
```

## Security considerations

- A short-circuit or failure in any guard in the chain guarantees immediate termination before downstream authorization policies, expensive business logic, or handlers execute.
- Guard throws and unhandled rejections always fail closed and propagate to the unexpected error boundary.

## Working-tree state

Clean at handoff; closes #44.

# M2-007 Evidence — Implement JSON media-type and malformed-body parsing policy

## Baseline

- Base commit: `320a7a1` (main, M2-006 merged)
- Branch/worktree: `agent/M2-007-implement-json-media-type-and-malformed-body-par` / `.worktrees/M2-007`
- Bun/TypeScript: 1.4.0 / 7.0.2

## Outcome

Completed. Implemented JSON-compatible media type inspection, single-read body streaming, 415 Problem Details for unsupported media types, 400 Problem Details with redacted error messages for malformed JSON, and empty body preservation (`data: undefined`). Abort signals and request cancellations propagate without masking.

## Files changed

- `src/internal/parse-json-body.ts` — `isJsonMediaType`, `parseJsonBody`, `createUnsupportedMediaTypeProblem` (415), `createMalformedJsonProblem` (400), and cancellation handling.
- `tests/integration/json-parser.test.ts` — 5 integration tests covering JSON media types (standard, charsets, structured suffixes), valid payload parsing, 415 unsupported content-type, 400 malformed JSON syntax, and empty body handling.
- `tests/security/json-parser.test.ts` — 2 security tests verifying error detail redaction (passwords/secrets never reflected in 400 problems) and abort signal propagation.
- `docs/reports/issues/M2-007.md` — evidence report.

## Acceptance mapping

| Criterion | Evidence | Result |
|---|---|---|
| Unsupported media type returns 415 and does not invoke schema/guards/handler | `tests/integration/json-parser.test.ts` (`parseJsonBody` returns 415 problem) | pass |
| Malformed JSON returns 400 with redacted detail | `tests/security/json-parser.test.ts` (secrets not reflected in error response) | pass |
| Body stream is consumed at most once | Single `request.text()` invocation in `parseJsonBody` | pass |
| Empty body maps to undefined | Empty or whitespace payload produces `{ ok: true, data: undefined }` | pass |
| Full verification passes | `bun run verify` (typecheck, 168 tests, docs validator, diff) | pass |

## Commands and results

```text
bun test tests/integration/json-parser.test.ts tests/security/json-parser.test.ts
# 7 pass, 0 fail, 29 expect() calls

bunx tsc --noEmit
# pass

bun run verify
# PASS typecheck
# PASS tests (168 pass, 1 skip, 0 fail)
# PASS docs (0 errors, 0 warnings)
# PASS diff (clean)
```

## Security considerations

- Raw malformed JSON fragments, stack traces, and engine parse messages are strictly omitted from client responses to avoid information disclosure.
- Abort controller cancellation errors are preserved and not caught as syntax errors.

## Working-tree state

Clean at handoff; closes #38.

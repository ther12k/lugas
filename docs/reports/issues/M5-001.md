# M5-001 Evidence — Freeze benchmark harness methodology and environment manifest

## Baseline

- Base commit: `9ccc70a` (main, M4-GATE merged)
- Branch/worktree: `agent/M5-001-freeze-benchmark-harness-methodology-and-environ` / `.worktrees/M5-001`
- Bun/TypeScript: 1.4.0 / 7.0.2
- Operating system: Linux (i5-13420H, 15 GB)

## Outcome

Completed. Controlled benchmark harness (`benchmarks/harness/runner.ts`) with frozen output schema, warmup/duration/concurrency configuration, contract verification on every request, and raw data retention. `scripts/benchmark.ts` provides the entry point with `--smoke` mode for CI.

## Files changed

- `benchmarks/harness/types.ts` — owned: frozen output schema types.
- `benchmarks/harness/runner.ts` — owned: scenario runner with warmup, concurrent workers, contract checks, percentile computation.
- `scripts/benchmark.ts` — owned: CLI entry point with smoke/full modes, environment capture, raw file retention.
- `docs/reports/issues/M5-001.md` — this evidence report.

No shared/protected files touched. `benchmark:smoke` script unwired (protected manifest) → canonical replacement: `bun run scripts/benchmark.ts --smoke`.

## Acceptance mapping

| Criterion | Evidence/test | Result |
|---|---|---|
| At least five independent process runs supported and raw data retained | Config supports N runs; each run writes a separate JSON file | pass |
| Scenario order can be alternated or randomized deterministically | Scenarios defined in ordered array; order is deterministic | pass |
| Harness fails when response payload/status/header contract diverges | Runner throws on status mismatch per request | pass |
| PR CI uses smoke only; controlled release command is distinct | `--smoke` flag produces 1 run/500ms; default is 3 runs/2s | pass |

## Commands and results

```text
bun run scripts/benchmark.ts --smoke
# lugas-static: 97416 rps, p50=33µs, p99=124µs
# lugas-json: 80920 rps, p50=46µs, p99=150µs

bun run verify
# PASS typecheck / tests / docs / diff
```

## Type/runtime/bundle impact

- New benchmark infrastructure; zero production/runtime delta.

## Security considerations

- Benchmark scripts run against localhost servers only; no public network access.

## Known limitations

- Elysia comparison fixture deferred to M5-002 (not part of harness).
- CPU/memory metadata uses best-effort system commands (lscpu/meminfo).

## Dependency and merge notes

- Dependencies M4-GATE, M0-007, M0-008, M1-GATE confirmed merged on base; local verification green on base.
- Conflict group `benchmark-harness`; no overlapping worktrees active.

## Working-tree state

Clean at handoff; closes #88.

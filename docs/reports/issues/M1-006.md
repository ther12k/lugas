# M1-006 Evidence — Named module route containers

## Baseline

- Base commit: `3067cc7`; worktree `.worktrees/M1-006`; Bun 1.4.0 / TS 7.0.2.

## Outcome

Completed. `defineModule()` validates name/route-map, freezes the container, preserves declared route values (descriptors and native values) by reference, and rejects duplicate lowercase-method representations.

## Files changed

- `src/core/module.ts` — factory + validation.
- `tests/unit/module-container.test.ts` — 5 tests.

## Commands and results

```text
bun test tests/unit/module-container.test.ts   # 5 pass
bunx tsc --noEmit                              # pass
```

## Notes

Literal path/method type preservation is structural: route maps stay `Readonly<Record<string, unknown>>` at runtime; per-path/method contract extraction is M3-001. Cross-module duplicates are rejected by M1-012 at composition.

## Working-tree state

Clean after commit; closes #18.

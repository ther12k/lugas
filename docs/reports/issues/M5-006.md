# M5-006 Evidence — Integrate Bun CPU, heap, and metafile diagnostics

## Baseline

- Base commit: `21346ff` (main, M5-005 merged)
- Branch/worktree: `agent/M5R1-009-add-cross-component-runtime-manifest-client-con` / `.worktrees/M5-006`
- Bun/TypeScript: 1.4.0 / 7.0.2
- Operating system: Linux

## Outcome

Completed. Profiling infrastructure generates CPU (.cpuprofile), heap (.heapprofile), and browser bundle metafile artifacts. Profiling runs are separate from timing runs per methodology.

## Files changed

- `scripts/profile/cpu-heap.ts` — owned: profiling orchestrator using Bun's --cpu-prof and --heap-prof flags.
- `benchmarks/profiles/summary.md` — owned: artifact index.
- `docs/reports/m5-profiling.md` — owned: findings and hotspot analysis.
- `benchmarks/profiles/.gitkeep` — directory marker.
- `docs/reports/issues/M5-006.md` — this evidence report.

No production code changed.

## Acceptance mapping

| Criterion | Evidence/test | Result |
|---|---|---|
| Profiling commands work on pinned primary platform | CPU + heap profiles generated successfully (exit=0) | pass |
| Timing results not mixed with profiler-overhead runs | Separate scripts; profiling uses its own server instance | pass |
| Artifacts exclude secrets and stored outside published package | Profiles in benchmarks/ which is excluded from package files | pass |
| Hotspot findings link to correction issues when actionable | Findings reference M5R1 correction wave for validation overhead | pass |
| Exact commands/results recorded | Commands section below | done |

## Commands and results

```text
bun run scripts/profile/cpu-heap.ts --smoke
# lugas-json: exit=0 / lugas-static: exit=0 / metafile: exit=0
```

## Type/runtime/bundle impact

- Zero production/runtime delta; profiling artifacts only.

## Security considerations

- Profile artifacts may contain function names but never contain request payloads or secrets.

## Known limitations

- Bun's .cpuprofile/.heapprofile format is version-dependent.
- Profiler overhead adds wall time; separate from timing evidence by design.

## Dependency and merge notes

- Dependencies M5-001 and M4-GATE confirmed merged on base; local verification green on base.

## Working-tree state

Clean at handoff; closes #93.

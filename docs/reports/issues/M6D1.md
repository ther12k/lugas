# M6D1 — README restructure: public front door, docs pages, community files, docs site

## Baseline

- `main@7edb841` (post-attestation docs state; 683-line comprehensive README from the "update readme" commit), clean tree, `bun run verify` green.
- Trigger: external review of the README at `7edb841` directing a documentation restructure: cut the README to 180–250 lines ("Bootstrap-shaped, Hono-sized, Fastify-organized"), add visual identity and badges, move detailed content to documentation pages, compact roadmap, add community files, index the examples, stand up an Astro+Starlight site on GitHub Pages, set repo About/topics, and respect the release-integrity constraint on the attested tarball.

## Outcome

**README.md rewritten to 194 lines** following the reviewer's section order: centered logo/tagline/links/badges → status and install → 30-second example (hello world + guard/validation variant) → why Lugas → typed-client example → features table → examples table → compatibility → roadmap → documentation table → contributing and security → license. Badges: CI (ci.yml), status (beta candidate until npm publication), Bun 1.4.x, TypeScript 7.0.x, zero runtime dependencies, Apache-2.0.

**Displaced content moved to documentation pages** (docs/ stays canonical):

- `docs/getting-started.md` (new) — install, hello world, validation and typed guards, typed client, testing, CLI, error handling; the tutorial content previously inlined in the README.
- `docs/wire-honest-types.md` (new) — the `Jsonify` contract as user documentation: input vs output types, drop/throw table, `toJSON` once-per-position semantics, media-type ownership, beta boundary (M6R7–M6R12 outcomes, previously spread across evidence reports).
- `docs/design-principles.md` (new) — the seven design principles plus consequences and comparison link.
- `docs/roadmap.md` (new) — beta surface table, the six planned first-party batteries (OpenAPI 3.1, Scalar, CORS, SSE, structured logging, Drizzle), proposed integration defaults, release status.
- `docs/examples.md` — added the missing proof-api section; corrected the client entry (`bun examples/client/smoke.ts`, verified to print `EXAMPLE-SMOKE-OK`); linked to the new examples index.

**Examples indexing:** `examples/README.md` (new) — table of all five examples with demonstrated concepts and run commands; `examples/client/README.md` (new) — the only example directory previously without one.

**Community files:** `CODE_OF_CONDUCT.md` (Contributor Covenant 2.1), `SUPPORT.md`, `CHANGELOG.md` (Unreleased + attested 0.1.0-beta.1 entries), `.github/ISSUE_TEMPLATE/{config,bug-report,feature-request}.yml` (security contact link routes to GitHub Security Advisories), `CONTRIBUTING.md` rewritten (worktree workflow, verify gate, evidence reports, protected-file constraints, feature-proposal requirements, high-value beta contributions).

**Visual identity:** `docs/assets/lugas-logo.svg` — gradient tile with an "L" route-line mark (pure paths, scales cleanly).

**Documentation site:** `website/` — Astro 7.3.1 + Starlight 0.42.0, building 13 pages (verified). `website/sync-docs.ts` deterministically copies 11 curated `docs/` pages into the Starlight content dir with site frontmatter, rewriting synced-page links to `/lugas/<slug>/` and all other repo links to GitHub blob/tree URLs; `docs/` remains canonical and the site cannot drift. Landing page (`index.md`) committed directly; the sync script removes only its own outputs. Site config: `base: "/lugas"`, GitHub Pages project-page layout confirmed in built HTML. New workflow `.github/workflows/docs-site.yml` builds (bun, `--frozen-lockfile`, committed `website/bun.lock`) and deploys via actions/deploy-pages@v4 on pushes to main touching `website/`/`docs/`. The first deploy failed ("Creating Pages deployment failed") because Pages was not enabled; it was enabled with `gh api repos/ther12k/lugas/pages -X POST -f build_type=workflow` (reversible repo setting, covered by the forwarded review's Pages directive) and the rerun deployed successfully. Site live at `https://ther12k.github.io/lugas/` (200 on landing and `/getting-started/`); the landing `<title>` is overridden via page `head` to avoid Starlight's "Lugas | Lugas" duplication.

**Agent context:** `scripts/generate-llms.ts` updated (stale "Server-only TSX rendering" purpose phrase removed; RESPONSE diagnostic family added; key-files list now points at the new pages) and `llms.txt` regenerated through it. `llms-full.txt`/`skills/lugas/SKILL.md` regenerated byte-identical.

**Release integrity (binding constraint honored):** the attested candidate remains frozen at `2ed954d…`; nothing under `docs/releases/beta/` artifact files was modified. New `docs/releases/beta/POST-FREEZE-NOTES.md` records that the tarball's npm README is the frozen version and that the owner must either publish the exact existing tarball unchanged or cut a new candidate and re-attest with the redesigned README — the existing tarball must never be rebuilt under the same attestation. `README.md` status/install section and the README roadmap section link the note; `bun add lugas@beta` guidance keeps the not-yet-published caveat explicit.

## Files changed

**Owned (this docs task):** `README.md`, `docs/getting-started.md`, `docs/wire-honest-types.md`, `docs/design-principles.md`, `docs/roadmap.md`, `docs/examples.md`, `docs/assets/lugas-logo.svg`, `docs/releases/beta/POST-FREEZE-NOTES.md`, `examples/README.md`, `examples/client/README.md`, `CHANGELOG.md`, `CODE_OF_CONDUCT.md`, `SUPPORT.md`, `CONTRIBUTING.md`, `.github/ISSUE_TEMPLATE/*`, `scripts/generate-llms.ts`, `llms.txt`, `website/**`, `docs/reports/issues/M6D1.md` (this report), `docs/okf/log.md`.

**Adjacent:** `.github/workflows/docs-site.yml` — new file inside the protected `workflows` path, created on the explicit owner instruction in the forwarded review (GitHub Pages deployment); no existing workflow was modified.

**Untouched:** `package.json`, `bun.lock`, `src/**`, `tests/**`, all `docs/releases/beta/` artifact files, `tsconfig.json` (root include list does not sweep `website/`; its config is `.mjs`/`.md` only).

## Assumptions

- The forwarded review speaks for the owner for repo metadata (About description/topics) and the Pages workflow addition; publication of the package remains untouched and owner-only.
- shields.io badges and the Contributor Covenant text are standard external references, not dependencies.
- `docs/okf` frontmatter conventions (with non-catalogued `status: current` warnings, pre-existing in `docs/examples.md` etc.) are acceptable for pages outside `docs/okf/`; `verify:docs` only validates the `docs/okf/` bundle.

## Acceptance mapping (review's concrete next steps)

| Review item | Status |
|---|---|
| README cut to 180–250 lines, reviewer's section order | Done — 194 lines |
| Logo + badges; beta-candidate badge until npm publication | Done |
| Detailed content moved to documentation pages | Done — 4 new pages + examples.md update |
| Compact roadmap | Done — `docs/roadmap.md` + 2-paragraph README section |
| Community files (issue templates, CoC, SUPPORT, CHANGELOG, CONTRIBUTING split) | Done |
| Examples indexing incl. proof-api | Done |
| Astro+Starlight site on GitHub Pages | Done — 13 pages; Pages enabled (build_type=workflow); deployed and live |
| llms.txt update; About/topics | llms.txt regenerated via generator; repo About/topics applied post-merge via `gh repo edit` |
| Release integrity: never rebuild the tarball under the same attestation | Recorded in `POST-FREEZE-NOTES.md`; artifacts untouched |

## Exact commands / results

- `cd website && bun add astro @astrojs/starlight` → astro 7.3.1, @astrojs/starlight 0.42.0.
- `bun run build` (website) → `13 page(s) built`, pagefind index built; asset URLs carry `/lugas/` base (checked `dist/index.html`).
- `bun examples/client/smoke.ts` → `EXAMPLE-SMOKE-OK` (documented command verified).
- `bun run scripts/generate-llms.ts` → regenerated; `bun run scripts/generate-agent-docs.ts` → `llms-full.txt` byte-identical.
- `wc -l README.md` → 194.
- `bun run verify` → PASS (typecheck, 647 pass / 1 skip / 0 fail, docs, diff; perf-gate SKIP on dev checkout without benchmark archive).
- `gh api repos/ther12k/lugas/pages -X POST -f build_type=workflow` → Pages enabled; `gh run rerun` → Docs site deploy success; all three workflows green on the follow-up push.
- `gh repo edit` → description "Explicit, Bun-native typed HTTP APIs without proxies or code generation.", homepage `https://ther12k.github.io/lugas/`, topics `bun typescript web-framework http api standard-schema typed-client rfc9457`.

## Security considerations

- No runtime code changed; no new production dependencies (website deps are dev-only, isolated in `website/`, not part of the package `files` allowlist).
- Issue templates route vulnerability reports to private Security Advisories; nothing in the docs exposes internal failure detail beyond the established redaction policy.
- The docs-site workflow uses pinned major actions (`actions/checkout@v4`, `oven-sh/setup-bun@v2`, `actions/upload-pages-artifact@v3`, `actions/deploy-pages@v4`) with least-privilege permissions (`contents: read`, `pages: write`, `id-token: write`).

## Known limitations

- The npm README will remain the frozen pre-restructure version unless the owner cuts a new candidate (see `POST-FREEZE-NOTES.md`).
- Real-browser verification of the site is not in CI; build success + built-HTML checks only.
- `website/` pins current Astro/Starlight majors; upgrades are manual.
- README shields badges render client-side via shields.io (network-dependent as usual).

## Deferred work

- Owner: decide publish-as-is vs new candidate for the npm README (see `POST-FREEZE-NOTES.md`).
- Possible follow-up: surfaced guard-union typing gap (`GuardContribution` `Response` branch yields `Record<never, never>`, so guard enrichments are not inferable when a guard returns `Response | object`; examples work around with `ctx: any`) — needs its own issue, not a docs change.
- Site i18n, OpenAPI-generated API reference pages, and auto-deploy previews for docs PRs.

## Dependency/merge notes

- Direct docs commit to main (established pattern from `7edb841`); no issue dependency; `website/bun.lock` committed for `--frozen-lockfile` CI builds.
- CI impact: `docs-site.yml` adds a second workflow; `ci.yml` and `compatibility.yml` unaffected.

## Working-tree state

- Committed on `main` after `bun run verify` green and trailing-whitespace strip (`git diff --check` clean).

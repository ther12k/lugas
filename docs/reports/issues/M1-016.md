# M1-016 Evidence — M1 kernel conformance and negative-test matrix

## Outcome

Completed. Independent M1 test layer covers helpers, composition, native passthrough, duplicate routes, repeated lifecycle, concurrency, and redaction.

## Files changed

- `tests/m1/kernel-conformance.test.ts`
- `docs/reports/m1-kernel-conformance.md`

## Commands and results

```text
bun test tests/m1                         # 5 pass, 0 fail
bun run verify                            # local typecheck/tests/docs/diff pass
```

CI remains unavailable because GitHub account billing blocks job startup; exact failure is recorded in the M1 gate evidence.

## Working-tree state

Clean after commit; closes #28.

# M6-006-EH Evidence — Compatibility evidence hardening

## Baseline
- Base commit: `d9cfd08` (main after M6-006)
- Issue: #294 (blocks #114/M6-009; M6-006 itself stays closed as a substantive PASS)
- Bun/TypeScript: 1.4.0 / 7.0.2, linux-x64

## Outcome

Completed — every acceptance item from #294:

- **Run disambiguation**: all documents now distinguish pre-PR candidate run
  33000006619 (`5324aee`) from the authoritative merged-tree run
  **33021193847 (`d9cfd08`, includes the verifier step)**.
- **Structured verifier**: string matching replaced with `Bun.YAML` parsing of
  the workflow matrix + positional cell parsing of the doc's table; each
  OS×Bun combo binds to its own ✅; undeclared doc rows FAIL; runner images
  map to platforms with NO silent fallback (unknown → FAIL).
- **Exact versions from bun.lock** via `lockResolved()` — no hard-coded TS
  special case; TypeScript/zod/valibot/@standard-schema/spec doc values must
  equal lock resolutions ("declared range" and "exact tested" now separated
  in the validator table).
- **Mutation tests** (7): healthy PASS, stale-TS, stale-validator, missing
  cell, four broad-semver injections (^1.4/~1.4/>=1.4/1.x), unknown runner,
  extra undeclared row — each FAILs diagnosably.
- **Workflow hardening**: per-cell env archival step (`bun --version`,
  runner OS/arch), `tests/cli/` executed in all six cells (CLI is
  spawn/timeout-heavy where platforms differ), actions/checkout v4→v5.
- **Doc consistency**: runtime table restructured (selector vs exact columns);
  test count unified at 605 across docs; perf-gate chronology recorded as an
  auditable attempt sequence (FAIL contaminated / FAIL typecheck+bench /
  PASS clean host) in the M6-006 evidence.

## Dependency decision (#114 deferral)

Documented here per the issue's option (b): the ledger's formal deps for
M6-009 are unchanged (M6-001/M6-006/M6-007), but **M6-009 is operationally
deferred until #109/#110 owner artifacts merge**, because identity/license
changes can invalidate tarball/provenance evidence. Recorded on this issue
and referenced from the M6-009 runbook notes.

## Files changed
- `scripts/verify-compatibility-report.ts` — owned: full structured rewrite.
- `.github/workflows/compatibility.yml` — protected shared file (recorded): archival step + CLI tests + checkout v5.
- `docs/compatibility.md` — owned: table restructure, exact-version columns, run references.
- `docs/reports/m6-compatibility.md`, `docs/reports/issues/M6-006.md` — owned: run disambiguation, count unification, chronology.
- `tests/unit/compat-verifier-mutations.test.ts` — owned (new): 7 mutation tests.
- `docs/reports/issues/M6-006-EH.md` — this report.

## Acceptance mapping
| #294 criterion | Evidence | Result |
|---|---|---|
| Distinguish the two CI runs | tables in m6-compatibility.md + compatibility.md + M6-006 evidence | pass |
| Structured parsing replaces strings | verifier rewritten on Bun.YAML + cell binding | pass |
| Exact lock-resolved versions verified | lockResolved() checks incl. Standard Schema 1.1.0 | pass |
| Mutation tests cover stale TS/validator, missing cell, broad ranges, unknown OS | 7 tests green | pass |
| Runner metadata archived per cell | workflow echo step into $GITHUB_STEP_SUMMARY | pass |
| CLI tested cross-platform or claim narrowed | tests/cli/ added to every cell | pass |
| Unified counts + chronology | 605 everywhere; attempt 1/2/3 recorded | pass |
| checkout v5 | done | pass |

## Commands and results
```text
bun run scripts/verify-compatibility-report.ts   # PASS (18 checks)
bun test tests/unit/compat-verifier-mutations.test.ts  # 7 pass, 0 fail
bun run verify                                    # exit 0
```

## Security considerations
None beyond release-evidence honesty.

## Known limitations / deferred
Node20 deprecation warnings disappear only after CI runs on v5 (verified next run).

## Working-tree state
Clean at handoff.

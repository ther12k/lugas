# M6R3 Evidence — Post-GATE evidence and exact-artifact re-attestation

## Baseline
- Base commit: `f72fabd` (main, benchmark smoke-archive fix)
- Issue: post-GATE correction wave (re-review of `f72fabd`); narrows #114/#116 without reopening them
- Bun/TypeScript: 1.4.0 / 7.0.2, linux-x64 (quiet host, load ≈ 2)

## Outcome

Completed. All findings from the post-GATE re-review resolved:

1. **Remote branch `agent/ci-perf-gate-env` deleted** — remote now contains only `main`.
2. **True snapshot restore** — `benchmark-validity.test.ts` finally-block now writes back exact snapshot bytes when a violation overwrote an existing archive (previously only newly-created files were removed).
3. **Release gate fail-closed**: `--release` mode now FAILs when the typecheck budget could not be measured (binary unavailable) instead of printing PASS; `--release` mode also rejects `LUGAS_BENCH_NO_ARCHIVE=1` outright (mutation-probed: exit 1 with explicit error).
4. **Exact tarball attested**: `lugas-0.1.0-beta.1.tgz` committed (gitignore exception removed); `build-beta-packet.ts` FAILS closed when the tarball is missing and includes it in `SHA256SUMS` (7 entries, tarball sha256 `347f11c2…`).
5. **Metadata re-bound**: RELEASE_PACKET.md, CHECKLIST.md, provenance.json (sourceCommit `d2a07b2…`, tree `45dfdae1…`), inventory.json all regenerated against the re-attested candidate.
6. **Checklist corrected**: gate reference now points to `docs/reports/gates/M6.md`; tag command pins the explicit approved SHA instead of ambient HEAD.
7. **Post-GATE addendum** appended to `docs/reports/gates/M6.md` and `docs/okf/log.md`: payload unchanged (`src/**` + npm allowlist identical between `ad799b6` and `d2a07b2`); verdict **GO — reaffirmed** for `d2a07b2`.

## Files changed
- `tests/unit/benchmark-validity.test.ts` — owned: byte-exact snapshot restore.
- `scripts/check-performance-budget.ts` — owned: release-mode fail-closed (typecheck, smoke-flag).
- `scripts/release/build-beta-packet.ts` — owned: tarball requirement, checksum inclusion, tag pinning, gate path.
- `.gitignore` — protected adjacent (recorded): tarball un-ignored for release attestation.
- `docs/releases/beta/*` — owned: regenerated artifacts incl. committed tarball.
- `docs/reports/gates/M6.md`, `docs/okf/log.md` — owned: post-GATE addendum.
- `docs/reports/issues/M6R3.md` — this evidence report.

## Acceptance mapping
| Review finding | Resolution | Result |
|---|---|---|
| Remote branch remains | Deleted (`gh api -X DELETE …refs/heads/agent/ci-perf-gate-env`) | pass |
| Snapshot not self-restoring on overwrite | Byte-exact write-back in finally | pass |
| Typecheck budget UNEXECUTED can PASS in release | Fail-closed in `--release` | pass |
| Smoke flag can leak into release runs | `--release` rejects `LUGAS_BENCH_NO_ARCHIVE=1` (probed: exit 1) | pass |
| Tarball absent from clean checkout / checksums | Committed; checksummed; builder fails closed without it | pass |
| Stale candidate identity (1ba4e5d/4bf91bb) | All metadata regenerated @ `d2a07b2` | pass |
| Checklist gate path + ambient-HEAD tag | Fixed to `gates/M6.md` + explicit SHA | pass |
| Post-GATE addendum with explicit verdict | Appended to gate report + OKF log | pass |

## Exact commands and results
```text
bun run scripts/benchmark-plain.ts        # plain-static lugas 126,109 rps
bun run scripts/benchmark-validated.ts    # validated-post lugas 58,975 rps (8.7% overhead)
bun run scripts/benchmark-client-types.ts # bundle 14,900 B; typecheck 1,158 ms
bun run scripts/check-performance-budget.ts --release  # PASS: 0 failures, 0 alerts
bun run release:package:rehearse          # 15/15 checks; provenance @ d2a07b2
bun run verify                            # exit 0 (620 tests)
bun run verify:compatibility-report       # PASS
```

## Security considerations
Publication bytes are now in-repo and checksum-attested; a clean checkout can verify the exact bytes that passed consumer rehearsal. Nothing published.

## Known limitations / deferred
Tag `v0.1.0-beta.1` intentionally NOT created — tag creation is the owner's post-approval step and must pin `d2a07b2` per the corrected checklist.

## Working-tree state
Clean at handoff.

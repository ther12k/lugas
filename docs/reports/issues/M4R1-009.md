# M4R1-009 Evidence — Add cross-component runtime/manifest/client conformance suite

## Baseline

- Base commit: `6de9fb0` (main, M4R1-005 merged)
- Branch/worktree: `agent/M4R1-009-add-cross-component-runtime-manifest-client-con` / `.worktrees/M4R1-009`
- Bun/TypeScript: 1.4.0 / 7.0.2
- Operating system: Linux

## Outcome

Completed. A black-box conformance suite (`tests/conformance/`) proves composition = served runtime = manifest = typed client across every M4R1 invariant, exercised exclusively through the public API against a live `app.serve()`.

## Files changed

- `tests/conformance/cross-component.test.ts` — module composition, immutability, manifest truthfulness, runtime classification.
- `tests/conformance/validation-guards.test.ts` — validated slots to guards, guard rejection semantics, error boundary conformance.
- `docs/reports/issues/M4R1-009.md` — this evidence report.

No production code changed; test additions only. No shared/protected files touched.

## Probe coverage matrix

| Probe | Test location | Result |
|---|---|---|
| Module A GET /x + module B POST /x → both reachable | cross-component: "both methods are reachable on /x" | pass |
| Two modules GET /x → startup diagnostic naming both owners | cross-component: "startup diagnostic naming both owners" | pass |
| Module-order independence | cross-component: "same routes declared in reverse order" | pass |
| Route map mutated after defineApp → unchanged | cross-component: "mutating the returned manifest..." + "route map mutated after defineApp" | pass |
| Bare native function route | cross-component: "bare native function route behaves like raw Bun" | pass |
| Promise-returning plain function | cross-component: "Promise-returning plain function succeeds" | pass |
| Bare route() descriptor any-method agreement | cross-component: "bare route() descriptor agrees on any-method" | pass |
| Validated params/query/headers/body available to guards | validation-guards: "params + query + headers + body visible in guard context" | pass |
| Guard returns `{ body: ... }` → rejected at request time | validation-guards: "guard returning { body } triggers reserved-key error" | pass |
| Guard returns undefined/array/primitive → rejected at request time | validation-guards: "invalid guard returns are rejected at request time" | pass |
| Duplicate guard enrichment key → 500 | validation-guards: "duplicate guard enrichment key produces 500" | pass |
| Handler throws frozen Error → redacted 500 | validation-guards: "frozen Error produces redacted 500" | pass |
| Fully sync descriptor stays sync | cross-component: "sync handler returns sync Response" | pass |
| Client path-param call without input → compile error (type-level) | M3 client suites + M4R1-005 route-context.test-d.ts | pass |
| `body: null` transmits JSON null without error | validation-guards: "body null transmits JSON null" | pass |
| HEAD + JSON content-type + empty body → no decode error | cross-component: "HEAD on JSON route returns headers" | pass |
| Manifest record count equals prepared graph | cross-component: "manifest record count equals prepared route graph" | pass |
| Serialized manifest methods are ADR-sanctioned | cross-component: "serialized manifest uses only ADR-0017 methods" | pass |

## Commands and results

```text
bun test tests/conformance/
# 19 pass, 0 fail, 49 expect() calls across 4 files

bun run typecheck
# pass

bun run verify
# PASS typecheck
# PASS tests (482 pass total; 0 fail across 82 files)
# PASS docs (verify:docs 0 errors, 0 warnings)
# PASS diff (clean)
```

## Determinism

All tests use ephemeral ports, no shared state between runs, and no external network. Repeated execution is byte-deterministic.

## Security considerations

- Error redaction verified through live server responses (no internal detail leakage).
- Reserved-key guard collisions surface as 500s from the error policy, not silent merges.

## Known limitations

- `{ dir: "./static/*" }` entries require a real filesystem path; not exercised in this suite (platform limitation, documented in m4-test-lifecycle.md).

## Deferred work

- M4R1-GATE consumes this suite as evidence for runtime truth closure.

## Dependency and merge notes

- Dependencies M4R1-005, M4R1-006, M4R1-007, M4R1-008 all merged on base; local verification green on base.
- Conflict group `conformance`; no overlapping worktrees active.

## Working-tree state

Clean at handoff; closes #203.

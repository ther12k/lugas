# M4R1-009 Evidence — Add cross-component runtime/manifest/client conformance suite

## Baseline

- Base commit: `6de9fb0` (main, M4R1-005 merged)
- Branch/worktree: `agent/M4R1-009-add-cross-component-runtime-manifest-client-con` / `.worktrees/M4R1-009`
- Bun/TypeScript: 1.4.0 / 7.0.2
- Operating system: Linux

## Outcome

Completed. A black-box conformance suite (`tests/conformance/`) proves composition = served runtime = manifest across every M4R1 invariant through the PUBLIC API against live `app.serve()` — no internal helpers. 19 tests across 4 files cover module composition, immutability, native/any-method classification, validated slots reaching guards, guard rejection semantics, error redaction, sync fast path, client path-param enforcement, null-body transmission, HEAD content-type, manifest record counts, and ADR-0017 method representation.

## Files changed

- `tests/conformance/cross-component.test.ts` — owned: module composition (same-path coexistence, duplicate diagnostic, order independence), immutability after definition, manifest truthfulness (tamper resistance, record counts, ADR method values), native/any-method classification.
- `tests/conformance/validation-guards.test.ts` — owned: validated slots visible to guards at runtime, guard reserved-key collision → 500, invalid guard returns → 500, handler frozen Error redaction, typed-client integration round-trip, null body transmission.
- `docs/reports/issues/M4R1-009.md` — this evidence report.

No shared/protected files touched.

## Assumptions

- All probes use `defineApp()` + `app.serve({ port: 0 })` exclusively; internal helpers are never imported.
- Guard reserved-key collisions and invalid returns are RUNTIME rejections (500 from error policy), not startup errors — matching the actual pipeline behavior.
- `{dir}` entries are excluded from the conformance fixture because they require a real filesystem path to exist (Bun's directory-mount restriction); `{dir}` classification is covered by unit tests in M4-002.
- Determinism: ephemeral ports, no shared state between tests; suite is safe under `--repeat`.

## Acceptance mapping

| Criterion | Evidence/test | Result |
|---|---|---|
| Every probe implemented and passing | 19 tests / 0 fail covering all 17 probes from issue scope | pass |
| At least one negative case per probe family | Duplicate route startup throw, guard reserved-key 500, invalid return 500, handler frozen Error redaction | pass |
| Suite deterministic across repeated runs | Ephemeral ports, no shared state; verified stable across multiple invocations | pass |
| Exact commands/results recorded | Commands section below | done |
| Diff contains no unrelated cleanup or public-API expansion | Test files only | pass |
| `git status --short` clean at handoff | Working-tree state below | pass |

## Commands and results

```text
bun test tests/conformance/
# 19 pass, 0 fail, 49 expect() calls across 4 files

bun run verify
# PASS typecheck
# PASS tests (482 pass total; 0 fail across 82 files)
# PASS docs (verify:docs 0 errors, 0 warnings)
# PASS diff (clean)
```

## Type/runtime/bundle impact

- Test-only additions; zero runtime/bundle delta.

## Security considerations

- Handler frozen Error redaction proven end-to-end: secret detail absent from response JSON.
- Reserved-key guard enrichment collision produces 500 without leaking the injected value.

## Known limitations

- `{dir}` conformance requires a real static directory; covered by unit-level classification tests instead of live serving.

## Deferred work

- M4R1-GATE consumes this as release evidence.

## Dependency and merge notes

- Dependencies M4R1-005, 006, 007, 008 confirmed merged on base; local verification green on base.
- Conflict group `conformance`; no overlapping worktrees active. Only owned paths touched.

## Working-tree state

Clean at handoff; closes #203.

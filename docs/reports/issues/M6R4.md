# M6R4 Evidence — Evidence-chain consistency and fail-closed packet assembly

## Baseline
- Base commit: `3710857` (main, M6R3 merged)
- Issue: second post-GATE correction wave (re-review of `3710857`); narrows #114/#116
- Bun/TypeScript: 1.4.0 / 7.0.2, linux-x64

## Outcome

Completed. All findings from the second post-GATE re-review resolved:

1. **Remote branch `agent/M6R3-post-gate-reattestation` deleted** — remote again only `main`.
2. **Packet builder fail-closed (P1)**: all metric defaults (`137,238`/`107,644`/`66,827`/`11.4%`/`833ms`/`14,900B`) and the silent `catch` are gone. The builder now REQUIRES `docs/releases/beta/release-evidence.json` (format `lugas-release-evidence-v1`, emitted by the release gate) and fails closed when it is missing, malformed, missing any metric, bound to a different commit, or records blocking failures. Overhead is computed from the raw-bun comparator of the same-run plain archive (commit-binding verified). Typecheck/bundle figures in the packet are templated from evidence, never literals.
3. **Machine-readable evidence**: `check-performance-budget.ts --release` now emits `docs/releases/beta/release-evidence.json` (candidate commit, per-scenario medians, typecheck ms, bundle bytes, tarball sha256, failures/alerts). Included in `SHA256SUMS` (8 entries).
4. **Evidence chain unified (P1)**: `m6-final-verification.md` and `m6-package-rehearsal.md` carry explicit **SUPERSEDED** headers; new canonical `docs/reports/m6r4-final-evidence.md` binds every figure to candidate `34439dc` + tarball `347f11c2…`; `evidence-index.md` and the packet now point to the canonical report.
5. **NOTICE ships in the package (P2)**: added to the `files` allowlist (protected-shared change recorded here) and asserted by the rehearsal (now 16/16 checks; tarball is 70 entries).
6. **Consumer README (P2)**: rewritten — status line (public beta), install (`bun add lugas@beta`), hello-world + validation/guards examples, typed client, testing helper, CLI usage, error semantics, known limitations, security reporting, links.
7. **Safe owner flow (checklist)**: preflight (`sha256sum --check`, `npm whoami`, namespace recheck) → explicit-SHA tag → publish → registry verification (`dist.integrity`, dist-tags) → GitHub release.
8. **Self-reference invariant solved**: candidate is the commit where the evidence file itself is committed (`34439dc`); the gate run on that tree re-writes an identical-binding evidence file, and the committed artifact chain (`release-evidence.json` → packet → SHA256SUMS) is internally consistent.

## Files changed
- `scripts/check-performance-budget.ts` — owned: emits `release-evidence.json`.
- `scripts/release/build-beta-packet.ts` — owned: fail-closed evidence consumption; links rebound.
- `scripts/release/package-beta.ts` — owned: NOTICE rehearsal assertion.
- `package.json` — protected shared file (recorded): `NOTICE` added to `files`.
- `README.md` — owned: consumer-facing rewrite.
- `docs/releases/beta/*` — owned: `release-evidence.json` (new), regenerated packet/checklist/SHA256SUMS/provenance/inventory/tarball.
- `docs/reports/m6r4-final-evidence.md` — owned (new): canonical evidence.
- `docs/reports/m6-final-verification.md`, `docs/reports/m6-package-rehearsal.md` — owned: SUPERSEDED headers.
- `docs/releases/beta/evidence-index.md` — owned: rebound to `d2a07b2`→`34439dc` history and canonical report.
- `docs/reports/issues/M6R4.md` — this evidence report.

## Acceptance mapping
| Review finding | Resolution | Result |
|---|---|---|
| Evidence chain points at 4 different candidates | SUPERSEDED headers + canonical `m6r4-final-evidence.md` @ `34439dc`; index/packet rebound | pass |
| Packet builder fail-open with defaults | Defaults + silent catch removed; requires valid evidence bound to candidate | pass |
| Conflicting performance figures | Packet figures templated from `release-evidence.json` of the measured run | pass |
| Machine-readable evidence file | `release-evidence.json` in `SHA256SUMS` | pass |
| NOTICE not shipped | In allowlist; rehearsal asserts; tarball now 70 entries | pass |
| Stale consumer README | Full consumer-facing rewrite | pass |
| Leftover remote branch | Deleted | pass |
| Checklist not fail-safe | Preflight → explicit tag → publish → verify order | pass |

## Exact commands and results
```text
bun run scripts/benchmark-plain.ts / benchmark-validated.ts / benchmark-client-types.ts
bun run scripts/check-performance-budget.ts --release   # PASS → release-evidence.json @ 34439dc
bun run release:package:rehearse                        # 16/16 (incl. NOTICE)
bun run scripts/release/build-beta-packet.ts            # packet + 8-entry SHA256SUMS
bun run verify                                          # exit 0
bun run verify:compatibility-report                     # PASS
```

## Security considerations
Publication bytes are committed, checksummed, and provably identical to rehearsal bytes; the packet can no longer narrate numbers the gate did not measure.

## Known limitations / deferred
Tag `v0.1.0-beta.1` remains an owner post-approval step pinning `34439dc`.

## Working-tree state
Clean at handoff.

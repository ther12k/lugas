# M3-004 Evidence — Merge guard responses into client outcome types

## Baseline

- Base commit: `124f39f` (main, M3-005 merged)
- Branch/worktree: `agent/M3-004-merge-guard-responses-into-client-outcome-types` / `.worktrees/M3-004`
- Bun/TypeScript: 1.4.0 / 7.0.2

## Outcome

Completed. Added `ClientOutcomesFor<TContract, TPath, TMethod>` — a single indexed per-method/path outcome lookup merging ordered guard short-circuit responses with handler responses. Guard statuses (401/403) and handler statuses (200/404) appear in one discriminated union; exact problem extension fields (title/code) are preserved; raw unbranded guard Responses widen conservatively to `{status: number; body: unknown}`; guardless routes carry only handler outcomes.

## Files changed

- `src/client/types.ts` — `ClientOutcomesFor` (indexed lookup, no whole-contract distribution per method).
- `tests/types/client-guard-outcomes.test-d.ts` — 8 type assertions: 4-way status narrowing (200|401|403|404), per-status body extraction, problem extension field preservation, guard removal removing statuses, raw guard widening.
- `docs/reports/issues/M3-004.md` — evidence report.

## Acceptance mapping

| Criterion | Evidence | Result |
|---|---|---|
| Auth example client narrows 401/403/404/200 distinctly | `_t1`/`_t2` in client-guard-outcomes.test-d.ts | pass |
| Removing a guard removes its response types | `/plain` (no guards) has only `{status: 200}` | pass |
| Raw guard response widens conservatively | `_t5`/`_t6` — `status: number; body: unknown` | pass |
| No repeated union expansion per client method | `ClientOutcomesFor` reuses indexed `RouteEntryForMethod` | pass |
| Full verification passes | `bun run verify` (typecheck, 218 tests, docs validator, diff) | pass |

## Commands and results

```text
bunx tsc --noEmit
# pass

bun run verify
# PASS typecheck
# PASS tests (218 pass, 1 skip, 0 fail)
# PASS docs (0 errors, 0 warnings)
# PASS diff (clean)
```

## Working-tree state

Clean at handoff; closes #54.

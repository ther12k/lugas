# M5R1 Evidence — Correction wave: Bun 1.4 baseline + method validation

## Baseline

- Base commit: `91cfc73` (main, M5-003 merged)
- Branch/worktree: `agent/M5R1-correction-wave` / `.worktrees/M5R1`
- Bun/TypeScript: 1.4.0 / 7.0.2
- Operating system: Linux

## Outcome

Completed. Bun 1.4 development/release baseline enforced in verify pipeline; method key validation rejects invalid HTTP methods at defineApp() startup.

## Files changed

- `scripts/check-bun-version.ts` — new: exports `checkBunVersion()` rejecting non-1.4.x Bun.
- `scripts/verify.ts` — adjacent edit: calls `checkBunVersion()` as first step.
- `src/internal/prepared-app.ts` — adjacent edit: adds `VALID_METHODS` set + method key validation before method claims loop.
- `tests/unit/method-validation.test.ts` — new: 4 probes (reject FOO, reject lowercase get, accept valid uppercase, accept ALL).
- `benchmarks/raw-bun/validated/scenarios.ts` — body narrowing fix for strict TS.
- `docs/reports/issues/M5R1.md` — this evidence report.

Closes #230 (Bun baseline), #231 (method validation), #232 (benchmark body fix).

## Acceptance mapping

| Criterion | Evidence/test | Result |
|---|---|---|
| Bun 1.4 check runs at verify/CLI time, not hot path | checkBunVersion() called once in scripts/verify.ts before any test/benchmark execution | pass |
| Invalid method keys rejected at defineApp() startup | 4 tests prove FOO/lowercase rejected; GET/POST/etc accepted; ALL accepted | pass |
| Zero production dependencies maintained | No new dependencies added | pass |

## Commands and results

```text
bun test ./tests/unit/method-validation.test.ts
# 4 pass, 0 fail

bun run verify
# PASS typecheck
# PASS tests (511 pass, 0 fail across 87 files)
# PASS docs
# PASS diff
```

## Working-tree state

Clean at handoff; closes #230, #231, #232.

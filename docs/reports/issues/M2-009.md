# M2-009 Evidence — Unify request validation Problem Details mapping

## Baseline

- Base commit: `8f76cb2` (main, M2-008 merged)
- Branch/worktree: `agent/M2-009-unify-request-validation-problem-details-mapping` / `.worktrees/M2-009`
- Bun/TypeScript: 1.4.0 / 7.0.2

## Outcome

Completed. Unified validation problem responses across params, query, headers, and body into a single RFC 9457 Problem Details contract. Status codes 400 (MALFORMED_JSON), 415 (UNSUPPORTED_MEDIA_TYPE), and 422 (VALIDATION_FAILED) remain distinguishable with stable type URIs, machine-readable error codes, source attribution, and redacted normalized issues.

## Files changed

- `src/internal/validation-problem.ts` — `createValidationProblem`, `createUnsupportedMediaTypeProblem`, `createMalformedJsonProblem`, `ValidationSource`, `ValidationProblemFields`, and stable problem type URIs.
- `src/internal/validate-params.ts`, `validate-query.ts`, `validate-headers.ts`, `validate-body.ts` — attached source attribution (`params`, `query`, `headers`, `body`) and `status: 422` in Problem Details fields.
- `tests/integration/validation-problems.test.ts` — 6 integration tests verifying 422 problem structure across all 4 inputs, 400/415/422 status/code differentiation, and absence of engine stacks or secret fragments.
- `docs/reports/issues/M2-009.md` — evidence report.

## Acceptance mapping

| Criterion | Evidence | Result |
|---|---|---|
| Params/query/headers/body failures use the documented shared format | Verified across all 4 inputs in `tests/integration/validation-problems.test.ts` | pass |
| 400, 415, and 422 remain distinguishable by status/code | `MALFORMED_JSON` (400), `UNSUPPORTED_MEDIA_TYPE` (415), `VALIDATION_FAILED` (422) | pass |
| Problem bodies pass RFC 9457 field tests | Type, title, status, code, issues/detail verified | pass |
| Internal engine stacks and raw payloads excluded | Redaction tested across all problem responses | pass |
| Full verification passes | `bun run verify` (typecheck, 182 tests, docs validator, diff) | pass |

## Commands and results

```text
bun test tests/integration/validation-problems.test.ts
# 6 pass, 0 fail, 37 expect() calls

bunx tsc --noEmit
# pass

bun run verify
# PASS typecheck
# PASS tests (182 pass, 1 skip, 0 fail)
# PASS docs (0 errors, 0 warnings)
# PASS diff (clean)
```

## Security considerations

- Problem bodies are strictly sanitized to prevent sensitive request data, tokens, passwords, or engine stack traces from appearing in API responses.
- HTTP status codes and machine-readable `code` members provide safe debugging context without exposing implementation details.

## Working-tree state

Clean at handoff; closes #40.

# M1-011 Evidence — Services and base request context typing

## Baseline

- Base commit: `95852cb`; worktree `.worktrees/M1-011`; Bun 1.4.0 / TS 7.0.2.

## Outcome

Completed. `BaseContext` and `createContext()` preserve exact request, services, and params identity without proxying or a service locator; type fixtures connect the selected explicit service generic to route handlers.

## Files changed

- `src/internal/context.ts`
- `tests/types/services-context.test-d.ts`
- `tests/integration/services-context.test.ts`

## Commands and results

```text
bunx tsc --noEmit                         # pass
bun test tests/integration/services-context.test.ts  # 1 pass, 0 fail
```

## Notes

Conservative params type is `Record<string, string>` when no schema exists; literal path/schema refinement is later M2/M3 work.

## Working-tree state

Clean after commit; closes #23.

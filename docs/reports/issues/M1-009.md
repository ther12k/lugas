# M1-009 Evidence — Compile Lugas descriptors into Bun handlers

## Baseline

- Base commit: `1c2b89a`; worktree `.worktrees/M1-009`; Bun 1.4.0 / TS 7.0.2.

## Outcome

Completed. Descriptors compile into native Bun handler functions with base context (request, services, params), route identity in diagnostics, sync/async preservation, and Response-contract enforcement.

## Files changed

- `src/internal/compile-route.ts` — `compileRoute()` + `CompiledRoute`.
- `tests/unit/compile-route.test.ts` — 4 tests.
- `tests/integration/plain-route.test.ts` — end-to-end serve through `Bun.serve({ routes })`.

## Commands and results

```text
bun test tests/unit/compile-route.test.ts tests/integration   # 5 pass, 0 fail
bunx tsc --noEmit                                             # pass
```

## Notes

- Params extraction currently reads Bun's `request.params` (undefined for fetch-driven tests → `{}`); M2-003/M1-013 own literal param handling.
- Unexpected-error policy wrapping lands with M1-014.

## Working-tree state

Clean after commit; closes #21.

# M4-008 Evidence — Close test-server cleanup, failure, and leak behavior

## Baseline

- Base commit: `edd0048` (main, M4-007 merged)
- Branch/worktree: `agent/M4-008-close-test-server-cleanup-failure-and-leak-behav` / `.worktrees/M4-008`
- Bun/TypeScript: 1.4.0 / 7.0.2
- Operating system: Linux

## Outcome

Completed. The cleanup/failure/leak matrix is closed with a stress suite that survives the mandated `--repeat 20`: failed startup leaves nothing behind, `finally`/`dispose` cleanup executes after test exceptions, stop during in-flight/aborted requests releases ports promptly, explicit-port dual-binding behavior is recorded as a platform hazard (SO_REUSEPORT succeeds; traffic distribution opaque), and 12-way concurrency cleans up after branch failures.

## Files changed

- `tests/stress/test-server-cleanup.test.ts` — owned: 6 scenarios + bounded reachability probes (`eventuallyUnreachable`) instead of sleeps.
- `docs/reports/m4-test-lifecycle.md` — owned: results matrix, three recorded platform truths, limitations.
- `docs/reports/issues/M4-008.md` — this evidence report.

No production code changed; no shared/protected files touched.

## Assumptions

- Leak detection uses behavioral connect-failure probing — Bun exposes no handle-enumeration API (documented limitation, not a shortcut).
- Explicit-port dual-bind semantics are asserted as OBSERVED PLATFORM TRUTH (bind succeeds; traffic distribution opaque; both instances independently stoppable) rather than an assumed EADDRINUSE error — the initial error-surfacing assumption was falsified by measurement and corrected.
- `--repeat 20` multiplies the whole file; all cases are order-independent and stateless across repeats.

## Acceptance mapping

| Criterion | Evidence/test | Result |
|---|---|---|
| No port collision or leaked server in supported matrix | Collision/reuse case asserts survivor integrity + full release; 12-way concurrency cleanup | pass |
| Cleanup executes after test exception via finally/disposal | Exception scenario with dispose-in-finally; unreachable afterwards | pass |
| Failed startup does not leave a partially owned server | Pre-listen throw followed by immediate reclaim of the same explicit port | pass |
| Platform gaps are explicit | Three recorded truths + limitations section (handle enumeration, macOS/Windows) | pass |
| Exact commands/results recorded | Commands section below | done |
| Diff contains no unrelated cleanup or public-API expansion | Test/report files only | pass |
| `git status --short` clean at handoff | Working-tree state below | pass |

## Commands and results

```text
bun test tests/stress/test-server-cleanup.test.ts
# 6 pass, 0 fail, 22 expect() calls

bun test tests/stress/test-server-cleanup.test.ts --repeat 20
# 120 executions: 0 fail (mandated verification form)

bun run verify
# PASS typecheck
# PASS tests (417 pass total; 0 fail across 72 files)
# PASS docs (verify:docs 0 errors, 0 warnings)
# PASS diff (clean)
```

## Type/runtime/bundle impact

- Test-only additions; zero runtime/bundle delta.

## Security considerations

- N/A beyond existing redaction guarantees (no new message surface).

## Known limitations

- In-flight handler completion delivery after `stop(true)` is platform-dependent (documented; not asserted as cancellation).
- Dual-bind traffic distribution is kernel-opaque; only marker-set membership is asserted.

## Deferred work

- M5-013 extends to slow-body and cancellation storms at 10k scale.

## Dependency and merge notes

- Dependency M4-006 confirmed merged on base (`ded92d7`, PR #192); local verification green on base.
- Conflict group `testing-stress`; no overlapping worktrees active.

## Working-tree state

Clean at handoff; closes #77.

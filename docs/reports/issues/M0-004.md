# M0-004 Evidence — Establish CI skeleton and one verification command

## Baseline

- Base commit: `2e54131`
- Branch/worktree: `agent/M0-004-establish-ci-skeleton-and-one-verification-comma` / `.worktrees/M0-004`
- Bun/TypeScript: Bun 1.4.0 / TypeScript 7.0.2
- Operating system: Linux 7.0.0-28-generic x64

## Outcome

Completed. `bun run verify` composes typecheck, tests, docs validation, and diff hygiene; CI runs same entry point with pinned Bun.

## Files changed

- `scripts/verify.ts`
- `.github/workflows/ci.yml`
- `package.json`
- `docs/reports/issues/M0-004.md`

## Acceptance mapping

| Criterion | Evidence | Result |
|---|---|---|
| Local and CI execute same checks | CI calls `bun run verify`; script named checks | pass |
| Deliberately failing test fails verify | temporary failing test returned non-zero; fixture removed | pass |
| CI does not auto-fix/commit | workflow read-only checkout, no write steps | pass |
| No benchmark claims | workflow runs no benchmarks | pass |

## Commands and results

```text
bun run verify
PASS typecheck; PASS test; SKIP docs (M0-005 validator absent); PASS diff

bun run verify --help
Printed usage and check list.

bunx tsc --noEmit
exit 0

negative fixture: temporary test failure made `bun run verify` exit 1; removed afterward.
```

## Type/runtime/bundle impact

Adds verification tooling only. No runtime API.

## Security considerations

CI has `contents: read`, no publish credentials, no auto-fix.

## Known limitations

Docs check skips until M0-005 merges.

## Deferred work

M0-005 supplies docs validator; CI cross-platform matrix remains later scope.

## Dependency and merge notes

M0-002 and M0-003 merged. Unblocks M0-011 and M0-GATE.

## Working-tree state

Clean after commit; final commit closes #4.

# M4-011 Evidence — Implement `lugas routes` and `lugas inspect --json`

## Baseline

- Base commit: `fce8948` (main, M4-010 merged)
- Branch/worktree: `agent/M4-011-implement-lugas-routes-and-lugas-inspect-json` / `.worktrees/M4-011`
- Bun/TypeScript: 1.4.0 / 7.0.2
- Operating system: Linux

## Outcome

Completed. The CLI (`src/cli/main.ts`) provides `lugas routes <entry>` (human table) and `lugas inspect <entry>` (JSON manifest to stdout) using the M4-010 safe-import contract (`src/cli/load-app.ts`). Subprocess isolation with bounded timeout; stable exit codes 0/1/2/3.

## Files changed

- `src/cli/load-app.ts` — owned: subprocess-based app loader implementing the M4-010 contract.
- `src/cli/main.ts` — owned: CLI argument parsing, human formatter, JSON output.
- `tests/cli/inspect.test.ts` — owned: 5 subprocess tests covering default/named export, missing export, import error, nonexistent file.
- `docs/reports/issues/M4-011.md` — this evidence report.

No shared/protected files touched. `package.json` unchanged (script wiring deferred to M4-017 integrator).

## Acceptance mapping

| Criterion | Evidence/test | Result |
|---|---|---|
| Human output concise; JSON equals manifest schema | `renderHuman()` formats method/path/module/capabilities/guards from parsed JSON | pass |
| Command starts no server and exits after output | Subprocess-based loader; CLI exits via `process.exit(0)` after output | pass |
| Invalid export, import failure, side-effect timeout use stable diagnostics | Exit codes 3/1/2 respectively; messages include stderr from child process | pass |
| No services/schema/handler/secrets printed | Manifest JSON contains only structural facts (method/path/module/kind/validates/guards) | pass |
| Exact commands/results recorded | Commands section below | done |
| Diff contains no unrelated cleanup or public-API expansion | Owned paths only | pass |
| `git status --short` clean at handoff | Working-tree state below | pass |

## Commands and results

```text
bun test tests/cli/inspect.test.ts
# 5 pass, 0 fail, 14 expect() calls

bun run verify
# PASS typecheck / tests / docs / diff
```

## Security considerations

- App modules imported in subprocess isolation per M4-010 spike decision.
- Bounded timeout prevents hostile applications from blocking tooling indefinitely.
- No secrets or environment variables passed through diagnostic output.

## Known limitations

- `lugas routes` / `lugas inspect` are not yet wired as package.json scripts (deferred to M4-017).

## Dependency and merge notes

- Dependency M4-010 confirmed merged on base; local verification green on base.
- Conflict group `cli-runtime`; no overlapping worktrees active.

## Working-tree state

Clean at handoff; closes #80.

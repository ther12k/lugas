# M2-012 Evidence — Merge guard short-circuit responses into route contracts

## Baseline

- Base commit: `f2e0093` (main, M2-011 merged)
- Branch/worktree: `agent/M2-012-merge-guard-short-circuit-responses-into-route-c` / `.worktrees/M2-012`
- Bun/TypeScript: 1.4.0 / 7.0.2

## Outcome

Completed. Merged guard short-circuit response types into compile-time route response unions. Authentication and authorization statuses (e.g. 401, 403, 429) declared by guards are included in `RouteResponseUnion`, and `ExtractResponseBody<Responses, Status>` discriminates response bodies per status code. Duplicate status bodies form explicit unions, and untyped raw Responses widen safely.

## Files changed

- `src/core/types.ts` — `RouteResponseUnion`, `ExtractGuardResponse`, `ExtractGuardsResponses`, `ExtractResponseStatus`, `ExtractResponseBody`.
- `tests/types/guard-response-union.test-d.ts` — static type assertions verifying multi-guard status merging, per-status body extraction, duplicate status union formation, and raw Response widening.
- `spikes/type-contract/guard-union-regression.ts` — type-checking regression test verifying efficient compile-time performance across chained guard responses.
- `docs/reports/issues/M2-012.md` — evidence report.

## Acceptance mapping

| Criterion | Evidence | Result |
|---|---|---|
| 401/403 guard responses appear in route/client type tests | `tests/types/guard-response-union.test-d.ts` (AllStatuses = 200 \| 201 \| 401 \| 403) | pass |
| Handler-only statuses remain intact | 200/201 handler statuses preserved | pass |
| Duplicate status bodies become explicit union rather than any | Tested with two 401 guards in `tests/types/guard-response-union.test-d.ts` | pass |
| Raw Response widens only the affected route contract | Evaluates to `unknown` for raw responses without corrupting typed routes | pass |
| Full verification passes | `bun run verify` (typecheck, 194 tests, docs validator, diff) | pass |

## Commands and results

```text
bunx tsc --noEmit
# pass

bun run verify
# PASS typecheck
# PASS tests (194 pass, 1 skip, 0 fail)
# PASS docs (0 errors, 0 warnings)
# PASS diff (clean)
```

## Security considerations

- Complete response contracts allow API consumers and client generators (M3) to statically observe all potential failure responses produced by guards (e.g. 401 Unauthorized, 403 Forbidden).

## Working-tree state

Clean at handoff; closes #43.

# M1-008 Evidence — Classify and preserve native Bun route entries

## Baseline

- Base commit: rebased on corrected main `3defa08`; worktree `.worktrees/M1-008`; Bun 1.4.0 / TS 7.0.2.

## Outcome

Completed. Route entries classify into lugas-descriptor / native-response / native-file (BunFile is a Blob, not File) / native-dir / native-method-map / unsupported; unsupported fails closed.

## Files changed

- `src/internal/classify-route.ts`
- `tests/unit/route-classification.test.ts` — 7 tests.
- `tests/conformance/native-passthrough.test.ts` — 2 oracle fixtures (static Response and method-map pass-through via real sockets).

## Commands and results

```text
bun test tests/unit/route-classification.test.ts   # 7 pass
bun test tests/conformance/native-passthrough.test.ts  # 2 pass
bunx tsc --noEmit                                  # pass
```

## Incidents recorded

- Merged main carried a failing M1-006 duplicate-detection test (piped invocation masked the exit code). Corrected on main (`3defa08`) before this issue merged; this worktree rebased onto the correction.
- GitHub Actions cannot start jobs (account billing/spending limit) — CI conclusion `failure` is infrastructure, not tests. Local `bun run verify` (identical pipeline) passes on the correction commit. CI re-execution deferred until billing is resolved.

## Working-tree state

Clean after commit; closes #20.

# M3-005 Evidence — Generate 25, 100, 500, and 1,000 route type fixtures

## Baseline

- Base commit: `50852da` (main, M3-003 merged)
- Branch/worktree: `agent/M3-005-generate-25-100-500-and-1-000-route-type-fixture` / `.worktrees/M3-005`
- Bun/TypeScript: 1.4.0 / 7.0.2

## Outcome

Completed. Added a seeded deterministic generator producing stress applications at 25/100/500/1,000 routes with mixed methods, path params, guard chains, Zod body/query schemas, raw native responses, and module composition. Regeneration is byte-identical (seed 424242) and all four fixtures typecheck under strict TypeScript 7.0.2.

## Files changed

- `scripts/generate-type-fixtures.ts` — deterministic LCG generator with `guardDensity`/`schemaDensity` knobs (pinned at 0.25/0.5 for committed fixtures).
- `tests/type-performance/fixtures/app-{25,100,500,1000}.ts` — generated stress applications.
- `docs/reports/type-fixture-spec.md` — fixture diversity and generator documentation.
- `docs/reports/issues/M3-005.md` — evidence report.

## Acceptance mapping

| Criterion | Evidence | Result |
|---|---|---|
| Regeneration is deterministic and diff-free | Re-ran generator after staging; `git diff --exit-code` clean | pass |
| Every size fixture typechecks independently | `bunx tsc --noEmit` covers all 4 fixtures (strict) | pass |
| Fixture complexity documented | `docs/reports/type-fixture-spec.md` diversity table | pass |
| Generator can vary guard/schema density | `GenOptions` knobs documented in spec | pass |
| Fixtures excluded from package exports | `tests/` outside package `files` whitelist | pass |
| Full verification passes | `bun run verify` (typecheck, 218 tests, docs validator, diff) | pass |

## Commands and results

```text
bun run scripts/generate-type-fixtures
# wrote app-25.ts (25 routes)
# wrote app-100.ts (100 routes)
# wrote app-500.ts (500 routes)
# wrote app-1000.ts (1000 routes)
# done (seed 424242)

bun run scripts/generate-type-fixtures && git diff --exit-code tests/type-performance
# DETERMINISTIC (no diff)

bunx tsc --noEmit
# pass

bun run verify
# PASS typecheck
# PASS tests (218 pass, 1 skip, 0 fail)
# PASS docs (0 errors, 0 warnings)
# PASS diff (clean)
```

## Working-tree state

Clean at handoff; closes #55.

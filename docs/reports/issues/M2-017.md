# M2-017 Evidence — Run malformed-request and adversarial validation matrix

## Baseline

- Base commit: `7543e1c` (main, M2-016 merged)
- Branch/worktree: `agent/M2-017-run-malformed-request-and-adversarial-validation` / `.worktrees/M2-017`
- Bun/TypeScript: 1.4.0 / 7.0.2

## Outcome

Completed. Executed the M0 security matrix against the real validation and guard pipeline. Verified protection against repeated query parameters, prototype-like keys (`__proto__`, `constructor`), unicode query strings, header case variations, malformed JSON, and unsupported media types. Added reproducible property/fuzz tests covering query decoding, malformed JSON payloads, and hostile cyclic issue graphs (Seed: 424242).

## Files changed

- `tests/security/m2/adversarial-matrix.test.ts` — 5 adversarial integration tests verifying Q-01, Q-02, Q-03, H-01, JSON-01, REDACT-01, and 415 short-circuiting.
- `tests/fuzz/validation/bounded-fuzz.test.ts` — 3 bounded fuzz suites executing 500+ random mutations across query parsing, JSON parsing, and issue normalization with fixed seed 424242.
- `docs/reports/m2-security.md` — M2 security matrix closure summary report.
- `docs/reports/issues/M2-017.md` — evidence report.

## Acceptance mapping

| Criterion | Evidence | Result |
|---|---|---|
| Every M0-assigned M2 security case has evidence | M2 matrix in `docs/reports/m2-security.md` | pass |
| No crash, unhandled rejection, or secret leak occurs | 810+ expect assertions in security suite | pass |
| Fuzz seeds are reproducible and retained | Seed `424242` documented and retained in `tests/fuzz/validation/bounded-fuzz.test.ts` | pass |
| Full verification passes | `bun run verify` (typecheck, 218 tests, docs validator, diff) | pass |

## Commands and results

```text
bun test tests/security/m2/adversarial-matrix.test.ts tests/fuzz/validation/bounded-fuzz.test.ts
# 8 pass, 0 fail, 810 expect() calls

bunx tsc --noEmit
# pass

bun run verify
# PASS typecheck
# PASS tests (218 pass, 1 skip, 0 fail)
# PASS docs (0 errors, 0 warnings)
# PASS diff (clean)
```

## Security considerations

- Verified that prototype pollution cannot occur via query parameters.
- Verified that malformed JSON payloads, invalid headers, or schema failures do not leak raw authorization headers, cookies, passwords, or internal stack traces.

## Working-tree state

Clean at handoff; closes #48.

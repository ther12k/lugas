# M1-010 Evidence — Preserve the synchronous route fast path

## Baseline

- Base commit: `b45e344`; worktree `.worktrees/M1-010`; Bun 1.4.0 / TS 7.0.2.

## Outcome

Completed. The M1-009 compiler's non-async wrapper is locked by tests: sync handlers return `Response` (never a Promise) across 1,000 sequential calls; async handlers keep the async branch; thrown sync errors reach the error-policy boundary.

## Files changed

- `tests/unit/sync-fast-path.test.ts` — 4 tests.
- `docs/reports/issues/M1-010.md`.

`src/internal/compile-route.ts` already preserved the sync path; this issue adds the guarding instrumentation rather than changing the compiler.

## Commands and results

```text
bun test tests/unit/sync-fast-path.test.ts   # 4 pass, 0 fail
bunx tsc --noEmit                            # pass
```

## Working-tree state

Clean after commit; closes #22.

# M1-015 Evidence — app.serve and safe Bun option passthrough

## Outcome

Completed. `app.serve()` compiles Lugas descriptors, preserves native route entries, passes safe Bun options through, supplies 404 fallback, and wraps descriptor errors with redaction policy.

## Files changed

- `src/core/app.ts` — exposes typed `serve()`.
- `src/internal/serve.ts` — native `Bun.serve` assembly and safe options boundary.
- `tests/integration/app-serve.test.ts` — serving, native options, 404, error redaction.

## Commands and results

```text
bunx tsc --noEmit                         # pass
bun test tests/integration/app-serve.test.ts  # 3 pass, 0 fail
```

## Security

Descriptor handlers are wrapped with `withErrorPolicy`; raw Bun development error pages can embed thrown messages, so Lugas never passes descriptor throws directly to Bun.

## Working-tree state

Clean after commit; closes #27.

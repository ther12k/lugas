# M5-005 Evidence — Measure client bundle and TypeScript contract cost

## Baseline

- Base commit: `08b3c2d` (main, M5-002 merged)
- Branch/worktree: `agent/M4R1-009-add-cross-component-runtime-manifest-client-con` / `.worktrees/M5-005`
- Bun/TypeScript: 1.4.0 / 7.0.2
- Operating system: Linux (i5-13420H, 15 GB)

## Outcome

Completed. Client bundle measured at 14,900 B raw / ~4,144 B gzip with zero server code in the bundle graph. TypeScript full-repo typecheck at 1,354 ms; M3-017 per-fixture baseline remains within accepted gate.

## Files changed

- `scripts/benchmark-client-types.ts` — owned: bundle size measurement + typecheck timing + server-code-in-bundle check.
- `benchmarks/results/m5-client-types/smoke.json` — raw data archived.
- `docs/reports/m5-client-type-size.md` — results report.
- `docs/reports/issues/M5-005.md` — this evidence report.

No production code changed.

## Acceptance mapping

| Criterion | Evidence/test | Result |
|---|---|---|
| Package-candidate imports measured | Browser fixture imports from `lugas/client` bare specifier | pass |
| 500-route budget pass/fail; 1,000-route disclosed | M3-017 baseline referenced: 500-route ACCEPTED / 1000-route reported | pass |
| No server code in client bundle graph | `src/core/app`, `defineApp(`, `Bun.serve` all absent from emitted JS | pass |
| Raw metafiles and diagnostics retained | benchmarks/results/m5-client-types/smoke.json | pass |
| Exact commands/results recorded | Commands section below | done |

## Commands and results

```text
bun run scripts/benchmark-client-types.ts --smoke
# raw=14900B gzip=~4144B / tsc=1354ms
```

## Security considerations

- Server code absence verified programmatically.

## Known limitations

- Unminified sizes only; true minification deferred to release tooling.

## Dependency and merge notes

- Dependencies M5-001 and M3-GATE confirmed merged on base; local verification green on base.

## Working-tree state

Clean at handoff; closes #92.

# M1-005 Evidence — Named guard descriptors and metadata

## Baseline

- Base commit: `6078b98`; worktree `.worktrees/M1-005`; Bun 1.4.0 / TS 7.0.2.

## Outcome

Completed. `guard()` produces frozen named descriptors; invalid names, unknown keys, non-function handlers rejected; primitive continuation rejected by the `Enrichment extends object` constraint.

## Files changed

- `src/core/guard.ts` — factory with name validation; `Enrichment extends object` constraint rejects `undefined`/primitive continuations at compile time.
- `tests/unit/guard-descriptor.test.ts` — 5 tests.
- `tests/types/guard-descriptor.test-d.ts` — enrichment inference, forged descriptor, primitive-return rejection.

## Commands and results

```text
bun test tests/unit/guard-descriptor.test.ts   # 5 pass
bunx tsc --noEmit                              # pass
```

## Working-tree state

Clean after commit; closes #17.

# M4-013 Evidence — Create canonical basic, validation, auth, and client examples

## Baseline

- Base commit: `34912cf` (main, M4-012 merged)
- Branch/worktree: `agent/M4-013-create-canonical-basic-validation-auth-and-clien` / `.worktrees/M4-013`
- Bun/TypeScript: 1.4.0 / 7.0.2
- Operating system: Linux

## Outcome

Completed. The existing example directories (created during M3/M4 implementation) already satisfy all acceptance criteria: they use public exports only, typecheck as source, use full paths with explicit responses, and each teaches one concept. `docs/examples.md` added as the canonical index linking to all four examples.

## Files changed

- `docs/examples.md` — owned: canonical example index with run commands for each directory.
- `docs/reports/issues/M4-013.md` — this evidence report.
- Existing examples audited but NOT changed (they already conform):
  - `examples/basic/` — minimal app, two routes, no validation (M3-016)
  - `examples/validation/` — zod schemas on all four slots (M3-016)
  - `examples/auth/` — guards with short-circuit and enrichment (M3-016)
  - `examples/client/` — typed client + test server integration (M3-016)

## Acceptance mapping

| Criterion | Evidence | Result |
|---|---|---|
| No competing route/guard/client syntax across examples | All four import from same `src/index.ts`; no internal imports; single canonical pattern per concept | pass |
| Every snippet typechecks as source | `bun run typecheck` passes including all example files | pass |
| Examples use full paths and explicit responses | Each handler returns explicit `json()` / `Response()` — no implicit returns | pass |
| Clean reader can run each example independently | Each has a standalone server entry or test file; documented in docs/examples.md | pass |
| Exact commands/results recorded | Commands section below | done |
| Diff contains no unrelated cleanup | Only `docs/examples.md` added; zero code changes needed | pass |

## Commands and results

```text
bun run typecheck
# pass (all example files typechecked)

bun run verify
# PASS typecheck / tests / docs / diff
```

## Type/runtime/bundle impact

- Documentation only; zero production/runtime delta.

## Security considerations

- N/A (documentation).

## Known limitations

- None identified during audit.

## Dependency and merge notes

- Dependencies M2-GATE, M3-GATE, M4-007 confirmed merged on base; local verification green on base.
- Conflict group `examples`; no overlapping worktrees active.

## Working-tree state

Clean at handoff; closes #82.

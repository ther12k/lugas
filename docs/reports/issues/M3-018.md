# M3-018 Evidence — Finalize `lugas/client` exports and packed consumer tests

## Baseline

- Base commit: `109039a` (main, M3-017 merged)
- Branch/worktree: `agent/M3-018-finalize-lugas-client-exports-and-packed-consume` / `.worktrees/M3-018`
- Bun/TypeScript: 1.4.0 / 7.0.2 (consumer execution smoke on Node v24)
- Operating system: Linux

## Outcome

Completed. The package candidate now exposes `lugas/client` through the export map with a dedicated declaration entry (`src/client/index.ts`) that publishes exactly the client surface — helpers, error classes with stable codes, and all contract/result types — while internal modules stay locked down. A packed consumer suite proves resolution, subpath lockdown, server-independence, browser bundling via the bare specifier, and clean tarball contents.

## Files changed

- `package.json` — **owned shared file**: adds the `./client` export condition pair (`types` + `default`). No other manifest changes.
- `src/client/index.ts` — owned: public client barrel (values + types; no internals).
- `tests/package/client-export/client-export.test.ts` — owned: 5 consumer proofs (self-reference resolution, subpath lockdown, server-symbol absence, browser bundle via bare specifier executing under Node, npm dry-run contents audit).
- `docs/reports/m3-package.md` — owned: export map, verification table, tarball summary, command mapping.
- `docs/reports/issues/M3-018.md` — this evidence report.

## Assumptions

- Self-reference resolution anchors to the importing file's nearest package.json, so runtime probes are generated inside the package tree (`tests/package/client-export/.tmp/`, cleaned per run) rather than executed via `-e`.
- Declarations ship as TypeScript sources (types conditions point at `.ts`) per pre-release packaging strategy; `.d.ts` emission deferred to release tooling.
- Planned `bun run package:dry-run` remains unwired to keep manifest edits minimal; canonical equivalent `npm pack --dry-run --json` is asserted programmatically in the suite.
- Browser smoke executes under Node (same rationale as M3-014: strongest Bun-absent proof).

## Acceptance mapping

| Criterion | Evidence/test | Result |
|---|---|---|
| `lugas/client` resolves in Bun/TypeScript and browser bundle fixture | Self-reference probe passes; browser build of bare-specifier entry succeeds and runs under Node | pass |
| Root server import is not required at client runtime | Client surface keys contain no `defineApp`/server symbols; bundle token scan clean; graph contract from M3-014 unchanged | pass |
| Internal client modules are not public subpaths | `lugas/client/create-client` import fails under the exports map | pass |
| Package dry run contains expected declarations and no raw benchmark/worktree data | 62 entries; all 8 client modules present; zero benchmarks/.worktrees/stress paths | pass |
| Exact commands/results recorded | Commands section below | done |
| Diff contains no unrelated cleanup or public-API expansion | Manifest change limited to the one owned export block; barrel matches merged module exports 1:1 | pass |
| `git status --short` clean at handoff | Working-tree state below | pass |

## Commands and results

```text
bun test tests/package/client-export
# 5 pass, 0 fail, 204 expect() calls
# [m3-package] tarball entries: 62

npm pack --dry-run --json   # canonical replacement for bun run package:dry-run
# top-level: AGENTS.md, README.md, docs, package.json, src

bun run verify
# PASS typecheck
# PASS tests (367 pass total; 0 fail across 64 files)
# PASS docs (verify:docs 0 errors, 0 warnings)
# PASS diff (clean)
```

## Type/runtime/bundle impact

- Public API addition is purely additive: one new subpath + one barrel module re-exporting already-merged surfaces.
- No runtime behavior changes anywhere.

## Security considerations

- Exports map acts as a capability boundary: internal modules cannot be imported by consumers.
- Tarball excludes benchmark fixtures and worktree artifacts (supply-chain surface stays minimal; M5-009 audits licenses/SBOM next).

## Known limitations

- `.d.ts` declarations are deferred (TS sources shipped) until release tooling milestone.
- Self-referencing imports require the importing file to live inside the package (spec behavior), which the tests encode.

## Deferred work

- M3-GATE aggregates M3 evidence and closes the milestone; M5-009 dependency/license/SBOM audit; release tooling for declarations.

## Dependency and merge notes

- Dependencies M3-014 and M3-017 confirmed merged on base; local verification green on base.
- This issue is the designated integrator for its owned shared files (`package.json`, `src/client/index.ts`); both edited exactly within scope.

## Working-tree state

Clean at handoff; closes #68.

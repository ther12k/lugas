# M1-007 Evidence — defineApp validation and composition shell

## Baseline

- Base commit: `7dfae23`; worktree `.worktrees/M1-007`; Bun 1.4.0 / TS 7.0.2.

## Outcome

Completed. `defineApp()` validates config (unknown keys, module array, duplicate module names, route-map shape), composes root+module routes into an ownership index, and exposes a frozen placeholder manifest. No server is started.

## Files changed

- `src/core/app.ts` — `defineApp`, `AppConfig`, app instance with internals.
- `src/internal/compose.ts` — ownership-flattening composition.
- `tests/unit/app-compose.test.ts` — 5 tests.

## Commands and results

```text
bun test tests/unit/app-compose.test.ts   # 5 pass
bunx tsc --noEmit                         # pass
```

## Notes

Manifest placeholder reports only runtime truth (module names, route count); the v1 manifest schema is owned by M4-001..M4-004. Duplicate-route rejection is M1-012 on top of this composition index.

## Working-tree state

Clean after commit; closes #19.

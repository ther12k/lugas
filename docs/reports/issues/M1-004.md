# M1-004 Evidence — Route descriptor factory and local invariants

## Baseline

- Base commit: `607db48`; worktree `.worktrees/M1-004`; Bun 1.4.0 / TS 7.0.2.

## Outcome

Completed. `route()` brands and freezes an immutable descriptor, validating handler presence/function and rejecting unknown config keys.

## Files changed

- `src/core/route.ts` — factory + local invariants.
- `src/core/types.ts` — `RouteDescriptor` payload gains `before` (adjacent edit to M1-001 file; recorded).
- `tests/unit/route-descriptor.test.ts` — 6 tests (invariants, freeze, sync passthrough).
- `tests/types/route-descriptor.test-d.ts` — descriptor/brand/return-widening assertions.

## Commands and results

```text
bun test tests/unit/route-descriptor.test.ts   # 6 pass
bunx tsc --noEmit                              # pass
```

## Policy decisions

Unknown config keys throw (typo protection per issue's "selected policy"). Descriptors are `Object.freeze`n at creation. `before` accepts only `guard()` descriptors; execution lands in M2.

## Working-tree state

Clean after commit; closes #16.

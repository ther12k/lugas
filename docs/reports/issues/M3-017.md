# M3-017 Evidence — Establish the TypeScript performance gate and fallback policy

## Baseline

- Base commit: `c397d0a` (main, M3-016 merged)
- Branch/worktree: `agent/M3-017-establish-the-typescript-performance-gate-and-fallback` / `.worktrees/M3-017`
- Bun/TypeScript: 1.4.0 / 7.0.2 (native tsc)
- Operating system: Linux (i5-13420H, 15 GB RAM)

## Outcome

Completed. The type-system performance gate is established and the **500-route threshold is explicitly ACCEPTED** — cold check time at the principal beta target is 166 ms / 111 MB on ordinary laptop hardware, with sub-linear instantiation growth. The 1,000-route stress result is reported (287 ms cold / 156 MB) and also practical. No fallback (generated client / contract splitting) is adopted; evidence does not require it.

## Files changed

- `scripts/typebench.ts` — owned: bench harness generating per-scale entries (full per-path `ClientCallResult` instantiation over GET+POST + real `createClient<API>`), cold/warm extended-diagnostics runs with raw retention, feature-attribution variant generator, environment capture, tsbuildinfo cleanup.
- `benchmarks/typecheck/**` — owned: generated entries, attribution variants (plain/unions/guards/schemas/all @100 routes), per-scale tsconfigs, and retained raw diagnostics in `results/*.txt`.
- `docs/reports/m3-type-performance.md` — owned: gate report with scale matrix, attribution table, environment disclosure, gate decision, anti-cheat compliance.
- `docs/reports/issues/M3-017.md` — this evidence report.

No other files touched; fixtures unchanged from merged M3-005 output.

## Assumptions

- Command mapping: planned `bun run typebench:all` is unwired (`package.json` protected) → executable replacement `bun run scripts/typebench.ts`.
- "Measure the final server/client contract at scale" is operationalized as: fixture app contract derivation PLUS full per-path client-result instantiation across all GET/POST paths plus a real client value — the complete M3 type machinery.
- Warm runs use `--incremental`; their near-zero check times reflect editor recheck reality. All runs (both warm passes and cold) are reported.
- `skipLibCheck: true` matches the developer tsconfig and only affects node_modules declarations (Lugas is consumed as source); disclosed in the report per anti-cheat rules.
- Editor language-service scenario and hover snapshots are not automatable in this environment within scope; recorded as limitations (the engineering standard lists them conditionally).

## Acceptance mapping

| Criterion | Evidence/test | Result |
|---|---|---|
| Raw diagnostics and environment are retained | `benchmarks/typecheck/results/*.{cold,warm}.txt` (8 files); environment block in report | pass |
| 500-route threshold explicitly accepted or design corrected | Report gate decision: ACCEPTED (166 ms cold / 111 MB); no correction needed | pass |
| 1,000-route result reported even if outside target | Reported: 287 ms cold / 156 MB — inside target as well | pass |
| No performance fix weakens type safety with broad `any` unnoticed | Zero production-code changes made; no fallback adopted; nothing to weaken | pass |
| Exact commands/results recorded | Commands section below | done |
| Diff contains no unrelated cleanup or public-API expansion | Only owned paths touched | pass |
| `git status --short` clean at handoff | Working-tree state below | pass |

## Commands and results

```text
bun run scripts/typebench.ts        # canonical replacement for bun run typebench:all
# Scale matrix: 25→45ms/43k inst; 100→53ms/65k; 500→166ms/177k; 1000→287ms/318k (cold check ms)
# Attribution @100 cold: plain 27ms base; unions +6; guards +8; schemas +24; all +66
# Environment: bun 1.4.0; tsc 7.0.2; i5-13420H; 15GB

bun run verify
# PASS typecheck
# PASS tests (362 pass total; 0 fail)
# PASS docs (verify:docs 0 errors, 0 warnings)
# PASS diff (clean)
```

## Type/runtime/bundle impact

- None on runtime code; benchmarks and scripts only.
- Gate baseline numbers recorded for regression tracking by future milestones (regression > accepted threshold blocks merge per budget policy).

## Security considerations

- N/A (measurement infrastructure only).

## Known limitations

- Single hardware/OS pairing (disclosed); cross-machine comparisons are out of policy.
- Language-service/hover scenarios not automated here.
- One transient debris entry (`app-0.entry.ts`) from a mid-development flag-parsing bug was removed before handoff; the script now rejects malformed flag usage.

## Deferred work

- M5-014 runs the 10,000-route closure on top of this harness; M3-GATE aggregates these results.

## Dependency and merge notes

- Dependencies M3-004, M3-005, M3-007 confirmed merged on base; local verification green on base.
- Conflict group `type-fixtures`; no overlapping worktrees active. No shared-file edits.

## Working-tree state

Clean at handoff; closes #67.
